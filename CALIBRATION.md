# 베이스라인 캘리브레이션 가이드 (신뢰성 = #1 레버)

> 3-AI 만장일치: 앱스토어 1위의 진짜 레버는 **신뢰성**. 지금 `data/jobs/*.json`의 baseline은
> **손추정(잠정)** 이라 note에 "캘리브레이션 필요"로 정직 표기돼 있다. 이 문서는 그것을
> **관측 가능한 실데이터로 보정**하는 방법 — 가짜 정밀이 아니라 진짜 근거를 갖추기 위한 경로.

## 왜 중요한가
- 유저가 매일 보는 숫자가 "그럴듯하게 지어낸 값"이면 며칠 내 들킨다 → 리텐션 붕괴.
- 우리가 내내 지킨 anti-fake-credibility의 마지막 조각: **baseline도 근거를 가져야 한다.**

## 입력 데이터 소스 (관측 가능)
| 신호 | 출처 | 무엇을 말해주나 |
|---|---|---|
| **태스크 구성·중요도** | O*NET (`Task Ratings`, `Work Activities`) / 워크넷·한국고용정보원 직무사전 | 직업을 어떤 업무로 쪼개고 각 비중(weight) |
| **루틴성/자동화 잠재** | O*NET `Work Context`, Frey-Osborne(2013) computerisation 확률, McKinsey/OECD 태스크 자동화% | 각 태스크가 얼마나 규칙적·자동화 취약한가 |
| **현재 AI 역량 커버리지** | 우리 크롤러의 top_drivers 누적(태스크별 근거뉴스 수·성숙도) | 실제 AI가 이 태스크를 지금 얼마나 건드리나 |
| **시장 신호** | 채용공고 추세(사람인/잡코리아 API), 임금·고용 통계(KOSIS) | 수요 변화(단, 경기·계절 조정 필수 — 단독 증거 금지) |

## 산식 (제안 — 가중 결합, 모두 0~100 정규화)
```
task_baseline = 100 * (
    0.40 * routineness        # O*NET 루틴성 + 자동화 잠재
  + 0.35 * ai_coverage        # 현재 AI 역량이 이 태스크를 덮는 정도(우리 누적 근거)
  + 0.25 * market_signal      # 채용·임금 추세(조정 후, 보수적으로)
)
task_ci = baseline_불확실성(데이터 출처 수·일치도로 산출, 8~14)
job_baseline = Σ(task_weight × task_baseline)   # 이미 scoring이 이 구조
```
- 가중치(0.40/0.35/0.25)는 **백테스트로 튜닝**(아래). 임의 고정 금지.

## 검증 (백테스트 — 가짜 아님을 증명)
1. 과거 12~24개월 주요 AI 사건 타임라인 구축(예: GPT-4, Sora, Devin 발표일).
2. 그 시점에 우리 엔진을 돌려 직무별 지수 변화를 산출.
3. **실제 노동시장 변화**(해당 직군 채용공고/임금 추세)와 상관 확인.
4. 상관이 약하면 산식 가중치·factor 재조정. **예측력 없으면 baseline은 신뢰 불가.**

## 적용 절차
1. 위 소스로 task별 `baseline`·`ci` 재산정 → `data/jobs/<job>.json` 갱신.
2. baseline에 `"calibrated": true`, `"source"`에 실제 데이터 출처(URL/버전) 명시.
3. note의 "손추정 — 캘리브레이션 필요" → "캘리브레이션 완료 (출처/일자)"로 교체.
4. `python3 tests/test_core.py`로 회귀 확인 + 백테스트 상관 기록.

## 현재 상태
- 10개 직업 전부 `calibrated: false`(손추정). 위 절차로 직군별 보정 → 출시 신뢰성 확보.
- **이 작업은 외부 데이터 접근(O*NET 다운로드·채용 API 키)이 필요** → 사용자 결정/연결 후 진행.
