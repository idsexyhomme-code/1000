# 커리어 시그널 (WorkRadar) — 프로젝트 캐노니컬 문서

> AI 직무대체 실시간 알림 서비스. 3-AI 자율 루프(Gemini 2.5 Pro = 카피/UX, Codex = 비판적 리뷰, Claude = 메인 빌더)로 진행.
> 이 문서가 단일 진실 소스(SSOT). 매 루프 이터레이션마다 갱신.

## 운영 원칙 (자율 루프) ★최우선
- **절대 멈추지 않는다.** 결정이 필요하면 셋(Gemini/Codex/Claude)이 판단해 **즉시 진행**. 사용자에게 질문해 블로킹하지 않음.
- 모든 자율 결정은 진행 로그에 `[자율결정] 무엇을/왜` 형식으로 기록 → 사용자가 **나중에 거부권(veto)·수정 가능**.
- 사용자가 1분 내 답 없으면 = 위임으로 간주하고 진행 (사용자 명시 원칙). 사용자가 끼어들면 그 즉시 반영.
- **Git:** 매 이터레이션 끝에 의미 있는 단위로 `git add -A && git commit` → 원격(origin)이 있으면 `git push`. 사용자가 모바일에서 `git pull`로 진행 확인. 원격 없으면 로컬 커밋만(나중에 일괄 푸시). **API 키/시크릿 절대 커밋 금지**(.gitignore로 보호). 커밋 전 정밀 스캔(오탐 방지): `git grep -InE "AIzaSy[0-9A-Za-z_-]{30,}|ghp_[A-Za-z0-9]{30,}"` — 매치 있으면 **실제 중단**하고 제거 후 진행.

## 전략 재정렬 (3-AI 만장일치, max-gear 조준) ★
- **"로직 물량 최대 = 앱스토어 1위"는 틀림** (Gemini 3.1 Pro·Codex·Claude 만장일치). 양으로 신뢰는 안 생기고 내부 자기만족·복잡도부채·가짜신뢰성 위험.
- **1위의 진짜 레버 = 공유(바이럴) 루프 × 신뢰성 × 리텐션.** 로직은 이 루프를 받치는 근거여야지 제품의 자랑거리가 아님.
- **Max 화력은 "더미"가 아니라 고레버리지 3곳에 조준:**
  1. **신뢰성/캘리브레이션** — 베이스라인을 O*NET·워크넷 실데이터로, 출처검증·태스크매핑 정확도, 사용자 피드백 캘리브레이션 루프(가짜 아닌 진짜).
  2. **초개인화 생존 액션플랜(해자) ✅** — `src/actionplan.py`(멀티에이전트 워크플로우 설계→구현→3렌즈 적대검증). 직무 score→이번주 실행 3가지(defend/pivot, 근거뉴스 결박, 실툴명, 난이도/시간). **3렌즈(근거성·일반성·안전) 만장일치 fix-first → 가짜 근거 합성 차단**: driver-task 일치검증, 무매칭 시 -1 정직표기+guardrail_ok=False, 교차오염 제거. Gemini 라이브+폴백 통과. **report 결과화면에 연결 ✅** — '이번 주 생존 액션플랜' 1개 무료 티저+2개 잠금(🔒)→유료CTA가 해제. 해자=수익화 지점 결박. **배치 Gemini 플랜 캐시 ✅** — 변동 시 make_action_plan(use_gemini=True) 캐시, /report가 주입(없으면 폴백). 라이브 확인: 드라이버 있는 직무=source=gemini, 드라이버 0=정직한 폴백(guardrail_ok=False, anti-fake-credibility 작동). **다음: ① ungrounded(guardrail_ok=False) 플랜은 유료잠금 대신 '이번 주 새 근거 없음' 톤으로(근거없는 조언 판매 방지) ② notify 푸시에 액션 1개 노출.**
  3. **공유·리텐션** — MBTI식 결과 시각화(공유 강제), 푸시 훅, 온보딩 3탭 이내.
- **함정 회피:** 근거 없는 처방/지표 30개/에이전트 토론/예측문장 자동생성 = 바쁜일. 출시 지연·API비용 폭증·피봇 마비.

## 0-A. 장기 로드맵 — 월 1,000만원 경로 (2026-06-24 확정, 사용자 veto 가능)
> 사용자 목표: 이 서비스로 **월 1,000만원**. 응원이 아니라 계산. 세계적 기업 노하우 중 *우리에게 실제로 맞는 것만* 차용.

- **정직한 출발점:** 동작 MVP(카톡봇+미니웹) + 112직업 웹 + 17직업 심층 + 정직성 엔진. **단, 배포 안 됨·유저 0·매출 0·표시점수 손추정(calibrated:false).** 갭은 실재하고, 이 로드맵이 그 갭을 메운다.
- **수익 구조 = 2-티어 (핵심 통찰):** P1에서 정한 **사람검토 패키지(`AI 시대 커리어 재설계` ₩99,000 1회성)**는 *지불의지 증명 + 신뢰/후기 자산*을 만들지만, **사람검토라 노동 병목(주 5명 ≈ 월 ₩2.1M 상한)** — 이것만으론 ₩10M 불가. 그래서:
  - **티어1(스케일):** 디지털 셀프 리포트/구독(노동 0, 무한복제). ₩9,900/월 기준 ₩10M = **유료 ~1,010명**(무료→유료 3% 가정 시 무료 ~34,000명). ← 바이럴 루프로 달성.
  - **티어2(증명·고마진):** ₩99,000 사람검토 패키지. 신뢰·후기를 만들고 티어1으로 유저를 끌어올리는 깔때기 상단.
  - **₩10M 산식:** 티어1 1,000명 + 티어2 주 5명 동시 ≈ ₩10M+. **1,000명은 정복 가능한 숫자**(한국 "내 직업 AI에 안 잘리나" 불안 = 수백만 시장).
- **세계적 기업 노하우 중 실제 적용분:** ① 블룸버그 = 누구나 보는 데이터를 *신뢰·속도·해석*으로 판다 → 우리는 "커리어 터미널" ② 듀오링고 = 불안/FOMO를 습관 루프로(압력지수 변동 알림 = 재방문 엔진=리텐션) ③ 토스 = 무서운 데이터를 한눈에(이미 UI 방향) ④ 글래스도어/링크드인 = **유저 기여 데이터 → 장기 해자**("내 직무에서 실제 자동화된 것" 제보 누적 시 추격 불가).
- **우리 장기 해자(한 문장):** **신뢰 + 캘리브레이션된 데이터 + 습관 루프.** 손추정을 O*NET·실채용·실임금으로 캘리브레이트하는 순간 "한국어로 검증된 직무 자동화 신호"는 우리만 갖는다.
- **0→1,000만원 시퀀스(각 단계가 다음의 전제 — 순서 건너뛰면 붕괴):**
  1. **배포** → verify: 실유저 첫 리포트 수신 (⚠️ 카톡 비즈채널/호스팅은 브라우저 단계 = 사용자 클릭 필요)
  2. **신뢰 해자** = 스텁 2개 실연동 + 캘리브레이션 → verify: 외부 교차참조 배지가 전 직업에 (← **본 틱 진척: 17/17 직업 AIOE 앵커**)
  3. **바이럴** = '전략가 타입' 공유카드 → verify: 공유→가입 전환 측정
  4. **리텐션** = 압력지수 변동 푸시 습관화 → verify: D7 리텐션 측정
  5. **수익화** = 티어1 페이월 + 티어2 패키지 → verify: 첫 결제
- **버려야 할 트랩(불가침):** 50페이지 종합백과 · 가짜 통계로 신뢰 부풀리기(상품 자체가 신뢰라 자살 + 한국 명예훼손·AI기본법 리스크) · 지표 30개 · 에이전트 토론 = 전부 바쁜일.

## 0. 락(lock)된 방향성
- **컨셉:** 직업별 'AI 압력지수' — 공포 훅 유지하되 모든 변동에 근거 뉴스 링크 + 영향 업무 + 신뢰도 표시. ❌'대체확률 48%'식 단정 절대 금지.
- **공유 장치:** MBTI식 '전략가 타입' 결과 리포트 (위협+기회 동시 제시 → 부끄럼 없이 공유).
- **유료 트리거:** 무료로 "내 지수 +7, 원인=OO업무" 확인한 직후 → "상위 5%의 대응법 3가지(유료)".
- **시장:** 한국 우선(카카오톡 채널봇 + 미니웹) → 글로벌(영어, 텔레그램/앱).
- **시드 직업:** 영상편집자, 주니어 개발자.
- **네이밍(확정 2026-06-21):** 영어/글로벌 = **WorkRadar**, 한국 = 커리어 시그널. (사용자 확정, JAB 폐기)

## 1. 제품의 심장 — AI 압력지수 산출 루브릭 v2 (Gemini+Codex 교차검증 반영)

> **v1→v2 핵심 피벗 (Gemini·Codex 독립 수렴):** "직업별 0~100 단일 숫자"를 메인에서 **격하**한다.
> 단일 숫자는 (a) 너무 확정적으로 보여 churn/소송 포인트가 되고 (b) 주관 점수+뉴스편향+불명확 baseline의 합성물이라 방어 불가.
> **메인 = 태스크(업무) 단위 · 근거등급 · 신뢰구간 · 기상예보식 밴드.** 숫자는 보조 지표로.

### 1.1 구조 (태스크 우선)
- 직업이 아니라 **`직무-태스크 매트릭스`** 가 기본 단위. (예: 영상편집자 = 컷편집/자막/색보정/디렉팅… 각각 별도 점수)
- 직업 점수 = **태스크 점수의 가중 평균**으로만 산출. 사용자에겐 "어떤 업무가 점수를 밀었는지"를 먼저 노출.
- 베이스라인은 임의값 금지 → O*NET/워크넷/채용공고/업무시간 비중 등 **관측 데이터**로 산식 공개, **신뢰구간 표기** (예: `62 ± 12`).

### 1.2 델타 산출 5요인 + 앵커 정의 (재현성 확보)
| 요인 | 범위 | 앵커 예시 (주관성 제거) |
|---|---|---|
| 근접도 | 0~3 | 핵심 업무 직접 타격=3, 주변=1, 무관=0 |
| 성숙도 | 0~3 | 3=상용 GA + 유료고객 + 반복 사용사례 3건↑ / 2=베타 / 1=데모 / 0=논문 |
| 도입신호 | 0~3 | 고객사 실명·계약·고용변화 확인=3. **'채용공고 감소' 단독 증거 금지** (계절조정+인접직군 대비+4~8주 추세 필요) |
| 비가역성 | 0~3 | 재숙련 매우 어려움=3 |
| 규모 | 0~2 | 대중 보급형=2, 니치=0 |

- **내부 점수는 실수(float)로 유지**, round() 정보손실 금지. 사용자 표시는 밴드/소수1자리 (`+2.4`, `상승 압력: 중간`).
- **이벤트 클러스터링:** 같은 `기술+벤더+영향태스크+주장유형`은 1건으로 dedup·합산 (논문→데모→베타→GA 중복가산 방지).
- 방향 태그: `automation`/`wage-pressure`/`tool-adoption`/`regulation`/`demand-growth`.

### 1.3 출처 티어링 (펌핑 방어)
- **Tier 1** 공시·정부통계·고객사 직접발표 / **Tier 2** 주요 언론 / **Tier 3** 벤더 보도자료·블로그·유튜브.
- **벤더 단독 출처 max +2.** Tier 3 누적 가중치 상한. 고객사 실명/계약/반복사용 확인 시만 상향.

### 1.4 감쇠/회귀 (운영자 임의조정 차단)
- **공개·고정 함수:** 증거 이벤트 half-life 90일 / 실고용지표 365일 / 규제 이벤트는 만료 전까지 유지.
- 회귀 기준 명시(baseline). 운영자 수동 보정은 반드시 `editorial override` 라벨 + 독립 감사 로그.

### 1.5 표현층 — 기상예보 메타포 (Gemini) + 다음행동 (Gemini)
- 0~100 대신 **AI 기상예보**: 맑음(0~25)/구름조금(26~50)/흐림(51~75)/태풍경보(76~100). 공유 쉬움, 압박 완화.
- 점수 옆 **타임라인 로그** `[+3] 5/12 Adobe Firefly 영상생성 기능 발표 (사유: 핵심업무 자동화 ↑)` → 근거 투명.
- ★**무력감 차단:** 고압력 사용자에겐 반드시 '다음 행동'(리스킬링/트렌드/대응법) 연결. 불안 → 건설적 에너지로 전환.
- 공식(`가중합/14*8`) 노출 금지. "정해진 5원칙으로 계량화한 **참고 지표**, 모든 변동 출처 100% 공개, 인위 보정 불가" 원칙만 소통.

### 1.6 법적/윤리 가드레일 ★한국 규제 실재 (today=2026-06-06)
- **AI 기본법 시행됨 (2026-01-22):** 고용·생계 영향 → '고영향 AI' 해석 여지. 모델카드/데이터출처/평가절차/오류정정 절차를 **초기부터** 구비.
- **개인정보보호법 §37-2 (자동화 결정):** 이 지표를 **개인에 대한 자동결정에 사용 금지** 명시. 설명·이의제기·정정·human review 프로세스.
- **명예훼손(한국은 사실적시도 위험):** 특정 개인/회사/학교/자격군 겨냥 표현 금지. "대체된다"❌ → "관측된 자동화 압력"⭕. 예측 아닌 지표임 명시.
- **불안상품화 방지:** 푸시 카피 공포 프레이밍 금지(`위험 급등`❌ → `관련 업무 자동화 신호 증가`⭕). 단일 숫자만 푸시 금지 — 근거·불확실성·방어역량 동반.

## 2. 진행 로그
- **[자율틱 06-24] 리텐션 FIX: 푸시 쿨다운(재알림 최소간격) 추가 — 불안상품화·스팸 방지 ✅:** [자율결정] 리텐션 엔진(batch.py) 점검. 기존 중복방지(`get_notified != snap_ts`)는 *같은 스냅샷* 재알림만 차단 → 어떤 직무가 연속 배치에서 +2.1, +2.3으로 움직이면 **각각 새 snap_ts라 하루에도 여러 번 푸시** = 음소거/이탈, §1.6 '불안상품화 방지' 위반, 우리가 키우는 리텐션 레버를 오히려 훼손. **수정:** `NOTIFY_COOLDOWN_DAYS=3` 쿨다운 — 최근 알림 후 3일 내 추가 푸시 보류. **단 날씨밴드 변화(맑음→흐림 등 진짜 중대 변화)는 예외로 즉시 통과**(`_in_cooldown`은 set_notified가 저장하는 ISO ts를 파싱). 캐시(전략가/액션플랜) 갱신은 `new_alert` 유지(쿨다운 중에도 최신화), 푸시만 `should_push`로 게이트. 93테스트(쿨다운: 이력없음 False·1일전 True·5일전 False 회귀잠금). 발송은 여전히 외부 게이트.
- **[자율틱 06-24] 공유(#1레버) FIX: 전략가 타입 폴백 다양화 + 정렬·일관성 정직성 버그 2건 ✅:** [자율결정] 루프 가동. 공유 엔진 점검 — **폴백 타입이 모든 직업에 "균형 전략가형 🧭" 하나만** 줘서 바이럴(친구가 "내 타입은?" 유입)이 죽어 있었음. **수정:** weather 밴드별 4개 고유 타입(맑음🛡️수호자/구름조금🧭항해사/흐림🏄서퍼/태풍🔧재설계), 개인화는 직업별 tagline이 담당. **정직성 버그 2건 동시 제거:** ①`tasks[:2]`가 압력순 정렬을 가정 → 저압력 업무를 "고압력"이라 부를 수 있던 잠재버그 → 압력순 sort 후 high/low 선정. ②타입을 index 재임계값으로 뽑으면 게이지의 weather와 모순(예 50.5=구름조금인데 흐림타입) → **이미 계산된 weather 필드에 직접 매핑**(타입↔날씨 일관성 0불일치). 17직업 전수 검증. **92테스트**(타입↔날씨 일치+최고압력 업무가 위협에=정렬 회귀잠금). ※시드직업이 34~72에 몰려 실제론 2밴드만 등장 — 더 세분화하려면 태스크-카테고리 데이터 필요(미래).
- **[1000세션 06-24, 사용자 "월 1000만원/장기플랜→진행해줘"] 장기 로드맵 락 + 신뢰 해자 진척(앵커 11→17) ✅:** [자율결정] (1) **§0-A 장기 로드맵** 신설 — 월 ₩10M을 2-티어로 정직하게 계산(티어1 디지털 ₩9,900×~1,010명 + 티어2 ₩99,000 사람검토 패키지, P1 오퍼와 정합), 세계기업 노하우 적용분(블룸버그/듀오링고/토스/링크드인), 0→1000만원 5단계 시퀀스, 해자=신뢰+캘리브레이션+습관. (2) **신뢰 레버 실행:** 무앵커 6직업(medium SOC 프록시: call-center/data-analyst/marketer/office-admin/sales-rep/teacher)을 `calibrate.py --apply-medium`으로 앵커링 → **17/17 전 직업 AIOE 외부 교차참조 배지 확보**(이전 11). **정직성 FIX(과대표기 차단):** 메인 배지가 medium을 high와 동급으로 표기하던 버그 → soc_confidence≠high면 "(대표 SOC 프록시·중간 신뢰)" 단서 노출(teacher 검증=표기됨, video-editor=표기 없음). calibrated:false·손추정 면책 유지(표시점수 미보정). **91테스트**(medium/high 배지 분기 회귀잠금 추가). ⚠️medium 앵커는 사용자가 onetonline.org에서 SOC 확정 권장(veto 가능). 푸시는 사용자 게이트.
- **[자율틱 06-23] viral FIX: 깨진 og:image 잠복버그 제거 ✅:** [자율결정] 공유 레버 점검 중 발견 — `_og_image_meta`가 REPORT_BASE_URL 설정 시 `/static/og.png`를 가리켰으나 **해당 파일도 /static 서빙 라우트도 없음** → 배포(REPORT_BASE_URL 설정) 시 카톡/SNS 링크 미리보기 이미지가 깨짐(viral 레버 훼손). **수정:** 명시적 `OG_IMAGE_URL`(절대 http(s) URL) 설정 시에만 og:image emit, 미설정이면 아예 안 냄(깨진 참조 0). render.yaml에 OG_IMAGE_URL 훅 추가. 90테스트(깨진참조 없음+명시URL일 때만 출력 검증).
- **[자율틱 06-23] 적대리뷰 정직성 FIX: 'p74' 모호표현 → '상위 N%' ✅:** [자율결정] 체크포인트 셀프 적대리뷰(Codex MCP 미연결이라 Claude가 수행). **진짜 결함 발견:** 신뢰배지·상세의 "노출 상위 p74"가 percentile 73.8(=상위 26%)인데 "상위 74%(저위험)"로 오독될 수 있음 → 정직성 위반 소지. **수정:** `상위 {round(100-percentile)}%`로 명확화(video-editor 상위26%·accountant 상위1%), 상세엔 백분위 원값도 괄호 병기(투명). 테스트도 새 문구로 갱신. 89테스트.
- **[자율틱 06-23] 신뢰 레버↑: 메인 리포트 외부데이터 교차참조 배지 ✅:** [자율결정] 물량→신뢰 전환. #2에서 만든 AIOE 외부앵커(11직업)가 상세에만 묻혀있던 걸 **메인 리포트(render_html)에 정직한 신뢰배지로 노출** — "이 직무 AI 노출은 외부 공개데이터 AIOE(Felten 2021)로 교차참조, 노출 상위 p{X}" + **"표시 점수는 아직 손추정(미보정)" 면책 동반**(과대표기 금지). **앵커된 직무만** 노출(비앵커=배지 없음, medium 보류직업도 없음), 상세 클릭 유도(리텐션). 신뢰 6→전진, 리텐션 보강. 89테스트(배지 게이팅+면책 검증).
- **[자율틱 06-23] 시드직업 카피라이터(copywriter) 추가 ✅:** [자율결정] AIOE SOC 명확매칭 직업 1개 확장. `data/jobs/copywriter.json`(6태스크: 광고카피85·블로그SEO82·제품설명88·이메일80·브랜드메시지45·방향조율40, 손추정 calibrated:false — LLM 직접대체 직군이라 높게, 브랜드판단·관계조율은 방어적으로 낮게) + 연쇄 아틀라스(painmap 3·pain_deepdive 1·pain_probe micro10+질문5) + soc_map 27-3043(high)→AIOE 앵커 p74.1. **엔진 시드 16→17**. journalist(기자)·marketer(리서치)와 구분. 88테스트, detail 렌더·앵커 검증. 푸시·게시는 사용자 게이트(상기).
- **[1000세션 06-23, 사용자 "1~5번 멈추지말고"] #1 채용/임금 실데이터 어댑터 ✅:** `deepdive.py` hiring/wage 스텁을 진짜 fetch 경로로 구현(`_http_get_json` stdlib urllib + `_fetch_hiring`/`_fetch_wage`). `HIRING_API_URL`/`WAGE_DATA_URL` 설정 시 실호출·스키마검증·실데이터 반환, 미설정·실패·스키마불일치는 None→정직 스텁(**숫자 합성 0**). report.detail_html이 실데이터면 공고증감/AI우대비율·중위연봉/YoY/프리미엄격차 렌더. 테스트(monkeypatch, 네트워크X): 실데이터+None폴백+스키마거부.
- **[1000세션 06-23] #2 AIOE 실데이터 캘리브레이션 ✅:** 공개 AIOE(Felten/Raj/Seamans 2021, github.com/AIOE-Data/AIOE) xlsx를 stdlib(zipfile+xml)로 CSV 변환→`calibrate.py --apply`. **9개 high직업에 index_anchor 부착**(percentile 상대순위만 저장, raw z-score는 라이선스로 미저장, CSV는 .gitignore). medium 6개는 사용자 확정 전까지 보류. job_soc_map에 `aioe_soc`(2010 SOC 빈티지 등가: junior-dev 15-1132·journalist 27-3022 high / data-analyst Statisticians 프록시 medium) 추가, calibrate가 aioe_soc 우선 조회. detail UI에 외부앵커(p순위·SOC·출처) 노출 + '상대 AI 노출이라 직접대입 안 함, 표시점수는 여전히 손추정' 면책. **calibrated:false 유지**(표시점수=태스크롤업이라 미반영 — 과대표기 방지).
- **[1000세션 06-23] #3 원클릭 배포 설정 ✅:** `render.yaml`(Render Blueprint: free·singapore·healthCheck=/api/wr/health·start=HOST=0.0.0.0 python3 src/server.py $PORT, 무료휘발/영속디스크 정직명시) + `Procfile`(Railway/Heroku류) + server.py PORT env 폴백(이식성). 서버 부팅·/api/wr/health·/report 200 스모크 검증. DEPLOY-CLOUD.md 옵션B에 Blueprint 경로 추가. ⚠️카카오 비즈채널/호스팅 계정은 브라우저 단계라 사용자 클릭 필요(터미널 불가).
- **[1000세션 06-23] #4 새 시드직업 부기·경리원(bookkeeper) ✅:** `data/jobs/bookkeeper.json`(6태스크: 전표입력85·대사78·청구72·급여70·경비분류75·월말결산45, 손추정 calibrated:false) + 연쇄 아틀라스 전부 정직하게 채움(painmap 3pains·pain_deepdive 대표심층1·pain_probe micro-itch10+검증질문5, **16/16 직업 커버리지 계약 충족**) + soc_map 43-3031(high)→AIOE 앵커 p76.6. 기존 9앵커도 정확한 AIOE citation 갱신. **엔진 시드 15→16**. 88테스트. ⚠️푸시는 하니스가 main 직접푸시 차단 → 로컬 커밋 4개 대기(사용자 `git push origin main` 필요).
- **[루프틱 06-22] UX↑(진입점):** 게임·데일리(릴스가 가리키는 바이럴 진입점)에 로딩 상태("Loading…") — "—" 깜빡임 제거, 첫인상 개선. 84테스트.
- **[루프틱 06-22] UX↑:** 직업 그리드 로딩 플레이스홀더("📡 Loading 300+ jobs…") — jobs.json 로딩 중 빈화면 첫인상 개선. calibrate는 한국직무용+실데이터 없어 보류(가짜신뢰 금지). 84테스트.
- **[루프틱 06-22] 인프라↑:** DEPLOY-CLOUD.md(Mac+ngrok 탈피 가이드 — 옵션A: ngrok고정+caffeinate(오늘추천) / B: Render+디스크 / C: Fly볼륨). 파일저장 휘발 함정 정직 명시. 인프라 3/10 전진.
- **[루프틱 06-22] 리텐션↑:** 데일리에 연속출석 스트릭(🔥 X일 연속+"내일도 와서 이어가", localStorage, 어제=연속/하루빠짐=리셋). 복귀훅=리텐션 3/10 전진. 84테스트.
- **[루프틱 06-22] 신뢰↑:** 결과에 "점수 계산법 투명 공개"(손추정·미보정 정직표기, 대부분 툴은 숨김=차별화). 신뢰 6/10 전진. 84테스트. 백엔드·터널 health OK.
- **[루프틱 06-22] 진짜도움↑:** 결과에 과업별 행동가이드(압력티어별: 고=AI 1차패스+검수 / 중=판단보유 / 저=해자라 더투자). 무료가치 깊이↑(해자 4/10 전진). 84테스트.
- **B10 (프로그래매틱 SEO — 3차 리뷰 정직반영) ✅ (2026-06-22, '월1000' 3차):** `app/job/[slug]` — 직업마다 정적 SEO 페이지 SSG(1,345개). 타이틀 "Will AI replace an ICU Nurse?"(a/an 교정) 검색의도 타겟 + 정직 teaser(실제 hi/lo 과제 분할·directional 면책) → 딥컨설팅 CTA(?job= 핸드오프). `sitemap.ts`(1,346 URL)+`robots.ts`. 빌드 1,352정적페이지, 프로덕션 검증(/job/icu-nurse·pet-groomer 200). **3차 리뷰 정직 처리:** ✅SEO(합법·정직 채널) 채택 / 🔴가짜 출처("WEF·McKinsey·GPT-4o 처리됨") 거부=거짓권위·존재이유 배신(실제 인용기사로 이미 권위) / 🔴가짜 B2B 대시보드 판매 거부=바이퍼웨어 / 🟡이메일수집·B2B문의=백엔드/검증 후. 게시(검증)가 여전히 1순위.
- **B9 (인기직업 세부 전문화 심화 1000→1345) ✅ (2026-06-22, 사용자 "세부 전문화 깊게"):** `tools/gen_specializations.py` — 34개 인기 부모직업(개발자·간호사·교사·변호사·디자이너 등)의 실제 하위역할 생성, **노출 차등 강제**. +345(중복0·형식불량0). 핵심 가치=같은 직군 내 AI노출 차이 가시화: 예 간호사 Informaticist 82·Telehealth 78(데스크/데이터) vs Psychiatric 12·Pediatric 15·ICU 28(현장/고위험/관계). 개발/엔지니어도 ML Eng 85 vs Bridge Inspection 18. 정직 분포 유지(avg 50.9, 낮430/중504/높411). 85테스트. 양쪽 배포·프로덕션 1345 확인. 플레이스홀더 "1,300+ roles". **판단: occupation×specialization 커버 충분 — 이제 게시(검증)가 1순위.**
- **B8 (직업 301→1000 확장) ✅ (2026-06-22, 사용자 "pet groomer 같은 니치 포함 1000개"):** `tools/gen_jobs.py` — Gemini로 24카테고리 배치생성→엄격검증(키/점수범위/task수)→id·name 중복제거→jobs.json 병합. 301→**1000**(+699), 형식불량 0. 정직 분포(base avg 50.8, 낮음287/중간455/높음258 — 다 높음 아님). 니치 포함(pet-groomer·equine-groom·exotic-pet-groomer·show-dog-handler 등). 점수는 directional 손추정(calibrated:false 원칙 유지). 85테스트 통과(엔진 로딩 무손상). web/en(Pages)+upsight/public 동기화, 양쪽 배포. 프로덕션 1000 서빙 확인. 플레이스홀더 "1000+".
- **B7 (게시 전 폴리시 + 외부 리뷰 정직 반영) ✅ (2026-06-22, '월1000' 리뷰):** ① **타이틀 버그** "Create Next App"→"WorkRadar — Is AI coming for your job?"(+OG/트위터). ② **마찰력↓** 인기직업 칩(무입력 진입)+직업 autocomplete 칩. ③ **흐름** funnel 도착 시 job+tasks+exp+ai 받아 **즉시 자동분석**(재입력0). ④ **정직 로딩** 터미널식 실파이프라인 로그(네 직업·과제 박힘) — 페르소나의 "가짜 논문스캔 중" 거짓문구는 **거부**(정직성 원칙), 실제 단계만 노출해도 신뢰효과 동일. ⑤ **레이트리밋** /api/assess IP당 10/h+전역 150/h(인메모리), 라이브검증 1~10=200/11~=429. **정직 반려(설익음):** $4.99 Stripe·B2B HR SaaS=검증 유저0이라 보류, 모욕 네이밍("엑셀노예")=브랜드("not a verdict") 위반이라 거부. 수익화는 의향캡처로 수요검증 먼저 권고. 라이브: upsight-blue.vercel.app.
- **B6 (영문 기사 실수집 + 게임→딥컨설팅 funnel) ✅ (2026-06-22, 사용자 "B4+funnel 연속"):** **B4** `collect.ts` — Google News RSS(무료·키불필요)로 `"직업명" (AI OR automation)` 최신 영문기사 수집→파싱→Article[]. route가 직업별 실수집(캐시), <3건이면 샘플 폴백. 라이브 검증: Graphic Designer(Coursera/WAFF…), Registered Nurse(WSNA/"can't call yourself a nurse"…) — perTask.why가 진짜 신호 인용(이전 video 실토 해소). **funnel** upsight `?job=` 프리필(핸드오프) + 라이브 퀴즈결과(web/en/index.html)에 "🔬 Go deeper—real AI analysis" CTA→upsight 직업핸드오프. 게임→index(기존)→딥컨설팅 체인 완성. 양쪽 배포(Vercel+Pages). 라이브 URL: **upsight-blue.vercel.app**. ⚠️남은것: 무료쿼터 레이트리밋·노출키 교체·정식 도메인.
- **B5 (딥 컨설팅 심화 + 로딩UX + 캐시) ✅ (2026-06-22, 사용자 "결과 너무 부실"):** ConsultSchema에 분석 축 추가(staysYours/aiTakes/perTask.why/skillMoves/horizon — 근거기반, 분량패딩X). 페이지에 stays/takes 2열·skill·horizon 섹션 렌더. 로딩 UX 단계표시(진행바+✓단계+경과초, 30s 멈춤느낌 해소). **인메모리 직업캐시**(같은 직업 평가 재사용→consult만): 라이브 1회차 39.8s/2회차 **16.9s**(<60s 한도 안전). 8필드 전부 채워짐 검증. ⚠️ perTask.why가 "evidence focuses on video"라 실토 → **샘플기사가 대상직업과 무관, B4 영문 실수집이 진짜 다음**. 재배포 완료.
- **B4-deploy (upsight 라이브 배포) ✅ (2026-06-22):** 301직업 선택 UI(`page.tsx`)+`public/jobs.json`+route `maxDuration=60`. 프로덕션 빌드 통과 → **Vercel 배포**(team video-roasterys-projects/upsight, 키=Vercel env GOOGLE_GENERATIVE_AI_API_KEY). Deployment Protection(인증벽) 사용자가 dashboard서 해제. **라이브 공개 검증:** `https://upsight-video-roasterys-projects.vercel.app` GET/ 200, 프로덕션 `POST /api/assess`(Graphic Designer) HTTP200·33s·개인화 결과 정상. ⚠️ **남은 한계(정직):** ① 근거 기사 = 아직 샘플 7개(한글) → 진짜 영문 실수집 필요(다음 B) ② 공개 API라 무료쿼터 소진/레이트리밋 가드 없음 ③ 노출키 폐기·교체 권장. ⚠️ upsight git remote 없음(로컬만).
- **B1–B3 (upsight 진짜 LLM 컨설팅 엔진 가동) ✅ (2026-06-22, 사용자 "더 디테일한 컨설팅"=B 선택):** Next.js+ai SDK `upsight/`의 멈춰있던 파이프라인을 라이브화. **B1** `app/api/assess` 라우트(타입체크 통과). **B2** Gemini **무료티어**로 실채점 검증 — 기사7개 추출→6개 관련판정→riskScore 3/5+subScores4종+근거기사 인용(HTTP200, 19s). 첫 유료키 잔액0(429)이라 **무료 새 프로젝트 키로 전환**(결제 미연결=무료티어). **B3** `consult.ts`+ConsultSchema — **개인화 영어 컨설팅**(yourRead/perTask 압력+조언/3단계plan/groundedIn 근거id), 라이브 검증(주니어·0-2y·AI매일·과제3개→과제별 high/med 차등+근거 a6, HTTP200, 33s). **정직성:** 근거기반·directional·과장0. 키는 인라인env만(코드/메모리 미저장, 노출키는 사용자 폐기 권고). ⚠️ **upsight=별도 git·remote 없음**(파이프라인 여태 미커밋이라 함께 로컬커밋). **다음:** ① 결과 화면(보이게) ② Vercel 배포(보려면, 사용자 계정) ③ B4 기사 실수집(영문 HN/RSS) ④ 라이브 Python 제품과 통합 여부 결정.
- **W11 (컨설팅 심화: 과제별 조언 엔진화 + 3단계 주간플랜) ✅ (2026-06-22, 사용자요청 "더 디테일한 컨설팅"):** `compute_result`에 ①`task_actions`(선택한 **모든** 과제에 압력구간별 맞춤 조언 high/mid/low — 프론트가 이미 클라이언트서 렌더하던 걸 엔진 진실원본화) ②`plan`(분기별 PLAN_TMPL 3단계, {top}/{low} 개인화) 추가. 결과 화면에 **"YOUR 3-STEP PLAN"** 블록 신설(free move 아래, 번호배지 3스텝). **정직성:** 입력(과제·경력·AI사용) 기반 조합형 심화지 분량패딩 X(career 스킬 "50p=트랩" 원칙 준수), directional·결과보장X. 루트 `CLAUDE.md`(LLM 코딩 4원칙) 신설. node 문법검사 통과·시크릿 깨끗·**85테스트**. ⚠️엔진/프론트 템플릿 중복 존재(B단계서 API 일원화 예정). **다음:** ① index.html 렌더 라이브 확인 ② upsight(Next.js+AI SDK) 엔진을 컨설팅에 실연결(진짜 LLM 근거 기반) ③ 게시·검증.
- **W10 (드롭박스식 referral 루프 — 고도화) ✅ (2026-06-22, 자율):** 추천 바이럴 루프 구현. 고유 초대코드(localStorage `wr_ref`), `?ref=` 추적(visit/signup), 결과에 "5갈래 전체 해제" 진행바+초대링크 → **3명 가입 시 5갈래 처방 전부 공개**(보상=우리 콘텐츠 원가0=드롭박스 공식의 안망하는 버전). 백엔드 append_referral/referral_count(iph 중복제거)·REFERRAL_GOAL=3·POST/GET /api/refer. **솔직 채점(vs 바이럴제품): 훅 8·공유성 8(referral로↑)·리텐션 3·신뢰 6·수익화 2·UX 7·인프라 3·검증 0 → 훅으로 ~7.5, 완성사업 ~4.** 격차=기능아닌 검증+수익화+인프라. **인프라 권고: Supabase "존나 사기" X, Render/Railway 무료티어에 Python 그대로(월 0~7천원) — 검증 후.** 85테스트. **다음:** 검증(올려서 K-factor)→터지면 referral 증폭. 게시는 사용자(브라우저 클릭 Claude 불가).
- **W9 (게임 랭킹👑 + 배포착수) ✅ (2026-06-22):** 게임 글로벌 랭킹(점수제출→#N of M, 1위 👑, 톱10 리더보드) 완성. 랜딩 게임 프롬넌트화(미끼 앞세움). 백엔드 자체 펀널(visits/quiz/sub) + ngrok 터널로 라이브 퀴즈↔Mac 연결됨(무료URL=ephemeral, 재시작시 변경 주의). CORS preflight 버그 수정. **배포 1차 시도:** 레딧 r/SideProject 게시했으나 신생계정 스팸필터에 삭제됨. **방안=web/en/DISTRIBUTION-PLAN.md**(계정워밍업·링크는 첫댓글·게이트낮은서브·modmail·댓글루트 / 메인은 틱톡·릴스(게이팅0)). 틱톡 업로드 대기. 83테스트. **다음(내일):** ① ngrok 고정도메인→API_BASE 영구 ② 틱톡 REEL1·2 게시 ③ 레딧 우회 게시 ④ Claude는 펀널로 반응 판정. ★Claude(터미널)는 브라우저 클릭 불가 — 게시는 사용자 또는 크롬확장 Claude.
- **W8 (라이브 신호 자동수집) ✅ (2026-06-21, 자율런):** 직군별 '왜 이 압력인가' 근거를 자동 신선화. `src/workradar_signals.py`: 구글뉴스 RSS 7피드→헤드라인→직군 분류(애매하면 버림)→`data/wr_signals.json` 캐시(실제 head/url만). `workradar.get_evidence/fresh_family_signal`: 신선 자동신호 우선, 없으면 큐레이션 폴백(항상 근거 존재). compute_result.evidence + 주간엔진이 사용. 라이브 검증(614항목 11직군 수집, auto=True). 오프라인 테스트 81. cron(매일 07:00) 운영. 캐시 .gitignore. **남은 자율후보:** band별 결과카피 심화·직업확장 / (로그인) 터널·Stripe·이메일발송.
- **W7 (직업 301개 + 리더보드 + 공유카드) ✅ (2026-06-21, 자율런):** 직업 213→**301개**(jobs.json, 전부 5task·정직분포, 13직군 키워드 전수매칭 general 0). **리더보드** `web/en/ranked.html`(301개 최고노출/최안전 토글+검색→퀴즈 유입, 공유·SEO 자산). **공유 카드** 결과에 canvas 1080² PNG 다운로드(타입·점수링·밴드·노출%·브랜드, IG스토리용). 랜딩에 🏆Leaderboard·▶Reels 링크. 80테스트, 라이브. **다음 자율후보:** 직업별 라이브 신호 자동수집(crawler), OG 동적이미지(서버필요), 결과 band별 카피 심화.
- **W6 (직업 112개 단일원본 + 상황별 다중 서비스) ✅ (2026-06-21):** 14→**112개 직업**. **단일 진실원본 `web/en/jobs.json`**(백엔드 workradar.load_jobs + 프론트 fetch가 같은 파일 → 드리프트 0, 폴백 있음). base 20~80 정직 분포(기술직·창작 높음, 간호·전기공 등 손작업 낮음=공유용 "넌 안전"). 프론트에 100+ **검색창**. 사용자 요청("한 직업에 하나만 제공 X") 반영: **BRANCH_SERVICES** — 5갈래(상황)마다 서로 다른 **3개 구체 서비스**({top}/{low}/{job} 개인화), compute_result에 services 포함, 결과 'Services for your situation' 렌더. 79테스트(100+·서비스3·분기별 상이). **다음 후보:** 직업별 라이브 신호 연동 / 터널 연결 / 결제.
- **W5 (심화 개인화: 경력+AI툴 + 점수분해) ✅ (2026-06-21):** 퀴즈 6단계로 확장 — 경력연차(0~2y/3~7y/8y+)·AI툴 사용(안씀/가끔/매일) 질문 추가. 점수 = 업무평균 + 경력보정(EXP_ADJ ±) + AI툴보정(AI_ADJ ±) → 같은 업무믹스도 갈림(주니어+AI안씀 93 Storm vs 시니어+AI매일 65 Cloudy). 보정이 분기에도 영향(rep는 최종점수 기준). 결과에 **'Why this score' 점수 분해**(factors: 업무/경력/AI툴 delta) 표시 = 블랙박스 아님=신뢰. compute_result(exp,ai 옵션) 하위호환, 진행점·문항번호 JS 자동6단계. 77테스트. **다음 후보:** 직업 더 추가 / 직업별 task 라이브뉴스 연동 / 터널 연결.
- **W4 (진짜 개인화 + 릴스 페이지) ✅ (2026-06-21):** 사용자 지적("같은 직업이면 다 똑같으면 뭐가 달라, 사람마다 다른데")을 반영. 퀴즈 질문2를 '반복도'→**'내가 실제 하는 업무 멀티셀렉트(1~3)'**로 교체. **점수 = 선택 업무들의 평균 압력** → 같은 직업도 사람마다 다름(예: 주니어개발자 CRUD위주=81 Storm vs 설계위주=28 Partly cloudy). 처방(move)이 본인 최고/최저압력 업무를 직접 지목, task bar에 선택업무 강조. `compute_result(job, tasks[], feel, inst)` 시그니처 변경(server/test/프론트 동기화). 릴스 `web/en/reels.html`(REEL1·REEL2 임베드, 랜딩 링크), mp4 git 추적·Pages 서빙. 76테스트. **다음 개인화 심화:** 경력연차·AI툴 사용여부 등 추가 입력 → 더 갈림(현재는 업무믹스 기반).
- **W1~W3 (WorkRadar US 백엔드 — 5갈래 코파일럿) ✅ (2026-06-21):** 시장 US/글로벌 피봇 + 이름 WorkRadar 확정. **W1 인터랙티브 퀴즈**(web/en/index.html): 직업→질문3개(반복도/분야감정/본능)→레이더스캔 애니→게이지 카운트업 리빌. 결과=**AI Risk Type 5갈래**(🛡️Defender/🔄Pivoter/🧗Reskiller/🦅Independent/🚀Founder)=공유정체성이자 커리어전략 분기. GitHub Pages 라이브. **W2 백엔드 엔진**(src/workradar.py): 서버측 진실원본(JOBS·BRANCHES·decide_branch·compute_result·band), 파일기반 저장(wr_quiz 완료로그·wr_subscribers 이메일구독, 원자적append+중복제거). server.py에 `POST /api/quiz`(계산+로깅)·`POST /api/subscribe`(이메일+동의+honeypot)·`GET /api/wr/health`·CORS+OPTIONS 추가. **W3 리텐션 엔진**(src/workradar_weekly.py): 구독자별 주간 다이제스트(압력+이번주신호 출처링크+분기+다음수+$29 CTA)→발송대기열(wr_outbox), --sample 미리보기. 퀴즈 프론트에 이메일 구독카드+best-effort 로깅(API_BASE 비면 mailto 폴백→퀴즈는 항상 작동). **RUN-BACKEND.md**: 24h Mac 셀프호스트(launchd KeepAlive)+터널노출(ngrok 고정도메인/Tailscale Funnel)+API_BASE 연결. **BUSINESS-DESIGN.md**: ₩1000만/월=$7.3k 단위경제 역산→하이브리드(무료퀴즈→무료주간이메일→$29 1회→$12/월 구독~600명). 정직성 유지(calibrated:false 손추정, PII .gitignore, 동의필수, 예측아닌 참고지표). 통합 스모크(퀴즈→구독→주간엔진→대기열) 통과, 72→76 테스트. **다음:** ① 사용자가 터널 띄우고 API_BASE 연결(로그인) ② 이메일 발송 provider(Resend/SMTP) ③ Stripe $29 Payment Link ④ 트래픽(릴스2개 게시+GoatCounter 등록).
- **P45 (paid pain 주문별 세부 이행 체크포인트 추적) ✅:** P44까지는 paid 주문의 큐/kickoff 누락을 복구할 수 있었지만, 결제 후 실제 운영에서 "자료 요청을 보냈는지, 고객 자료를 받았는지, 초안이 준비됐는지, 최종 전달했는지"가 `queued/working/delivered` 큰 상태만으로는 보이지 않았다. `CHECKPOINTS`와 `checkpoint_status`, `set_checkpoint` 추가: fulfillment_id 또는 payment order_id로 `kickoff_sent`, `materials_received`, `draft_ready`, `final_delivered`를 기록하며 note와 timestamp를 남긴다. `final_delivered` 체크포인트는 큐 상태도 `delivered`로 바꾼다. `paid_reconciliation`과 `report_markdown`, 콘솔 `report`에 `0/4 kickoff 발송`, `2/4 초안 준비` 같은 진행률과 다음 체크포인트를 노출. CLI `python3 src/fulfillment_queue.py checkpoint ORDER_ID kickoff_sent --note "자료 요청 발송"` 추가. 테스트 보강: order_id와 fulfillment_id 양쪽으로 체크포인트를 기록하고, 대조표에 다음 체크포인트가 바뀌며, 최종 전달 시 상태가 delivered가 되는지 검증. `DEPLOY.md`, `README.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 72개 통과, `python3 src/fulfillment_queue.py checkpoint --help` 확인. **다음:** 체크포인트 지연 경고 추가(예: paid 후 24시간 내 kickoff 미발송, kickoff 후 48시간 내 자료 미수신).
- **P44 (paid pain 누락 주문 repair-paid 멱등 복구) ✅:** P43에서 메모/주간 요약에 결제 후 이행 누락 경고가 뜨게 됐지만, 운영자가 큐 작업과 kickoff 파일을 직접 재생성하고 payment metadata까지 맞춰야 하는 불편이 남아 있었다. `save_paid_release_kickoff(out_path=...)`를 추가해 payment record에 남은 kickoff 경로 그대로 재생성할 수 있게 했고, `_paid_pain_payments`, `repair_paid_release_order`, `repair_paid_releases` 추가: paid pain payment record의 `pain_release_job/pain_release_pain`을 기준으로 `enqueue_paid_release`와 kickoff 생성을 다시 실행하고, 최종 payment event를 `src=repair-paid`, 올바른 `fulfillment_id`, `kickoff_path`로 append해 다음 reconciliation에서 같은 오류가 반복되지 않게 했다. CLI `python3 src/fulfillment_queue.py repair-paid ORDER_ID` 및 `repair-paid --all`, `--dry-run` 추가. 테스트 보강: 누락/잘못된 fulfillment_id/kickoff_path를 가진 paid 주문을 repair가 복구하고, 재실행해도 큐 작업이 중복 생성되지 않으며 reconciliation이 ok로 바뀌는지 검증. `DEPLOY.md`, `README.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 72개 통과, `python3 src/fulfillment_queue.py repair-paid --help` 확인. **다음:** paid pain 주문별 kickoff 발송/자료 수신/초안 전달 상태를 별도 체크포인트로 추적.
- **P43 (paid pain 이행 경고를 운영 메모/주간 요약에 자동 통합) ✅:** P42에서 `reconcile-paid` 단독 명령은 생겼지만, 운영자가 매일 `memo`나 `weekly`만 보고 있으면 결제 후 큐/kickoff 누락을 놓칠 수 있었다. `fulfillment_queue.operational_report`가 `paid_reconciliation` 결과를 함께 반환하도록 연결했고, `report_markdown`은 `paid pain orders/ok/issues` 요약 줄, 주문별 대조 표, `paid pain 이행 이슈` 섹션을 운영 메모에 기록한다. `memo_snapshots` 파서와 `weekly_summary`도 이 값을 읽어 최신 paid pain 주문/정상/이슈 수, 이슈 변화, 누적 이슈 카운트를 계산하고, `weekly_summary_markdown`에는 `paid pain 이행 경고`와 `reconcile-paid` 조치 문구를 자동 노출한다. 콘솔 `report`에도 paid pain 이행 대조 요약과 상세 확인 명령을 표시한다. 테스트 보강: 임시 payment/queue 파일 기준으로 missing queue/kickoff paid 주문이 운영 리포트, 저장 메모, 주간 요약, 주간 Markdown에 모두 경고로 이어지는지 검증. `DEPLOY.md`, `README.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 72개 통과. **다음:** paid pain 누락 경고가 뜬 주문을 복구하는 `repair-paid` 운영 명령 추가(큐/kickoff 재생성, 멱등 처리).
- **P42 (paid pain 주문 ↔ 큐/kickoff reconciliation) ✅:** P41에서 서명검증된 pain-paid 웹훅이 큐와 kickoff를 만들게 됐지만, 운영자는 `payments.jsonl`, `fulfillment_jobs.jsonl`, `kickoff-*.md`를 따로 확인해야 해서 결제만 되고 이행 큐가 빠진 사고를 놓칠 수 있었다. `store.payment_records(final=True)` 추가로 order_id별 최종 결제 이벤트를 조회할 수 있게 했고, `fulfillment_queue.paid_reconciliation`, `paid_reconciliation_markdown`, `print_paid_reconciliation` 추가: paid pain 주문만 골라 큐 작업 매칭, `fulfillment_id`/`order_id` 불일치, overdue, kickoff 파일 누락을 주문별 표와 issue_counts로 보여준다. CLI `python3 src/fulfillment_queue.py reconcile-paid` 추가. 테스트 보강: 정상 paid pain 주문 1건은 ok로 잡고, 큐/kickoff가 없는 paid 주문은 `missing_queue`, `missing_kickoff`로 잡는지 검증. `DEPLOY.md`, `README.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 72개 통과, `python3 src/fulfillment_queue.py --help`에 `reconcile-paid` 노출 확인. **다음:** `reconcile-paid` 결과를 운영 메모/주간 요약에 합쳐 결제 후 이행 누락을 매일 자동 경고.
- **P41 (pain-paid 웹훅 → fulfillment queue/kickoff 자동 연결) ✅:** P40까지는 결제 직후 킥오프 문서를 만들 수 있었지만, 서명검증된 paid 웹훅이 들어와도 운영자가 수동으로 큐와 킥오프 파일을 만들어야 했다. `fulfillment_queue.paid_release_id`, `enqueue_paid_release`, `save_paid_release_kickoff` 추가: pain-paid 주문은 `order_id` 기반 `fq_pay_*` id로 큐에 들어가며, PG 웹훅 재전송에도 중복 생성되지 않는다. 서버에 `PAIN_RELEASE_JOB`, `PAIN_RELEASE_PAIN` runtime 값을 추가하고, `_paid_pain_fulfillment_meta`를 통해 `status=paid`, `amount==PAIN_PAYMENT_EXPECTED_AMOUNT`, release 후보가 고정된 경우에만 자동으로 fulfillment job과 `data/fulfillment_reports/kickoff-ORDER_ID.md`를 생성한다. payment record extra에도 `pain_release_job`, `pain_release_pain`, `fulfillment_id`, `kickoff_path`를 남기고, 웹훅 응답에도 `fulfillment_id`를 반환한다. 범용 99,000원 결제나 failed/refunded 이벤트는 큐를 만들지 않는다. 테스트 보강: pain-paid paid helper가 큐 작업과 킥오프 파일을 만들고 연락처를 마스킹하며, 동일 order_id 재처리 시 중복 생성하지 않고, 범용 금액/failed 상태에서는 아무 것도 만들지 않는지 검증. `DEPLOY.md`, `README.md` 갱신. 검증: `python3 tests/test_core.py` 72개 통과. **다음:** paid pain 주문 처리 상태를 payments 리포트와 운영 리포트에서 한 화면으로 대조하는 reconciliation 리포트 추가.
- **P40 (pain 결제 직후 킥오프 문서 생성) ✅:** P39에서 실결제 후보를 고정해도, 결제가 들어온 직후 어떤 자료를 요청하고 3영업일 안에 어떤 순서로 처리할지 별도 문서가 없어 이행 지연 위험이 남아 있었다. `fulfillment.materials_for`, `fulfillment.kickoff_plan`, `kickoff_from_ids`, `kickoff_from_intent` 추가. `python3 src/fulfillment.py --job video-editor --pain revision-chaos --kickoff --sample` 또는 `--from-intent latest --kickoff`로 고객에게 보낼 자료 요청 메시지와 D0-D3 운영 체크리스트를 생성한다. 킥오프 문서는 기존 job/pain별 `수집할 자료`, `pain_probe.fulfillment_focus`, `artifact_adjustment`를 재사용해 자료 목록, 첫 확인 질문, micro-itch 전용 산출물 칸, D0 결제/신청 직후, D1 자료 검수, D2 산출물 초안, D3 QA/전달 절차를 포함한다. `--out`으로 `data/fulfillment_reports/kickoff-ORDER_ID.md` 같은 운영 파일에 저장 가능하며, 연락처는 마스킹된다. 테스트 보강: 킥오프 문서가 고객 자료 요청, 영업일 3일 납기 문구, D0-D3 체크리스트, 전용 산출물 칸, 전문판단/성과보장 제외 문구를 포함하는지 검증. `DEPLOY.md`, `README.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 71개 통과. **다음:** 결제 웹훅 paid 이벤트에서 pain release 후보와 order_id를 fulfillment queue에 자동 연결하는 운영 기록 보강.
- **P39 (pain-paid release candidate guard) ✅:** P38까지는 preview HTML을 만들 수 있었지만, 실결제 파일럿을 열 때 특정 후보를 고정했는지 자동으로 막지 못했다. `launch_preflight --mode pain-paid`를 강화해 결제 URL/금액/웹훅뿐 아니라 `PAIN_RELEASE_JOB`, `PAIN_RELEASE_PAIN`, `PAIN_RELEASE_PREVIEW`를 필수로 확인한다. guard는 직업군 id와 pain_id가 실제 `painmap`에 존재하는지, preview HTML이 읽히고 `</html>`로 끝나는지, `업무 고통 제거 파일럿`, 직업명, 약속 산출물, "선택 때문에 달라지는 결과물", 자동 전문판단/성과보장 제외 문구, `/privacy`·`/terms` 링크를 포함하는지 검사한다. 또한 `fulfillment.generate(..., sample=True)`가 해당 후보의 이행서 샘플을 만들 수 있고, 파일럿 약속 범위와 산출물/전문판단 제외 문구를 포함하는지 확인한다. 테스트 보강: pain-paid가 release env 없이 실패하고, 실제 preview HTML과 후보 env를 넣으면 통과하는지 검증. `DEPLOY.md`, `README.md` 갱신. 검증: `python3 tests/test_core.py` 70개 통과. **다음:** release 후보별 결제 후 첫 3영업일 이행 체크리스트/고객 자료 요청 템플릿 자동 생성.
- **P38 (제품화 후보 `/pain-offer` HTML preview export) ✅:** P37에서 제품화 micro-itch가 실제 `/pain-offer` 기본값으로 들어가게 된 뒤, 운영자가 결제 오픈 전에 화면을 직접 검토할 수 있게 확장. 운영 메모 파서가 `pain별 open` 섹션도 읽어 `weekly_summary.top_pains`를 계산하고, `productization_priorities` 각 후보에 대표 `pain_id`를 붙인다. `productization_draft_markdown`에는 preview할 Offer URL을 추가. `save_productization_previews`와 CLI `python3 src/fulfillment_queue.py productize-preview` 추가: 주간 제품화 후보 1~3개를 실제 `report.pain_offer_html` 렌더러로 HTML 저장하고, `data/fulfillment_reports/productization-preview-YYYY-MM-DD/index.md`에 후보별 파일명, demand_count, micro-itch, 산출물 위치, 필수 칸을 기록한다. 기본 preview HTML은 `01-video-editor-revision-chaos.html`처럼 `rank-job-pain.html` 이름으로 저장된다. 테스트 보강: 주간 요약의 `top_pains`, 후보 `pain_id`, 초안 Offer URL, preview HTML의 micro-itch/전용 산출물 칸/index 파일 생성 검증. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 70개 통과. **다음:** preview HTML을 검토한 뒤 특정 후보만 paid pilot로 승격하는 release checklist/guard 추가.
- **P37 (제품화 micro-itch를 `/pain-offer` 기본값으로 자동 반영) ✅:** P36의 제품화 초안이 Markdown에만 머무르지 않도록 서버 추천 경로에 연결. `pain_intents.recommended_micro_itches`에 `fallback_to_job` 옵션을 추가해 해당 pain의 실제 선택값과 직업군 전체 fallback을 분리했다. `fulfillment_queue.productized_micro_itches` 추가: 저장된 운영 메모의 `job::micro-itch` aggregate에서 특정 직업군의 제품화 후보만 읽고, atlas에 있는 문장만 노출한다. `server._recommended_micro_itches` 추천 순서를 ① 해당 pain의 실제 선택값 ② 주간 운영 메모 기반 제품화 micro-itch ③ 기존 직업군 aggregate fallback으로 변경. 따라서 `/pain-offer?job=video-editor&pain=revision-chaos`에 `mi=`가 없어도, 실제 pain-specific 신청이 없고 운영 큐 병목은 쌓인 경우 랜딩 hero, 작은 가려움 박스, 산출물 필수 칸/QA 섹션이 주간 1위 micro-itch 중심으로 자동 좁혀진다. 테스트 보강: 제품화 추천이 오퍼 HTML에 반영되고, pain-specific 실제 선택값이 생기면 운영 메모 추천보다 우선되는지 검증. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 70개 통과. **다음:** 제품화 초안의 hero/promise/CTA를 정적 샘플 HTML 또는 운영 preview 파일로 자동 export.
- **P36 (주간 1위 micro-itch → 좁은 오퍼 카피 초안 저장) ✅:** P35가 "무엇을 제품화할지"까지만 추천하던 상태에서, 운영자가 바로 랜딩/오퍼 문구로 옮길 수 있게 확장. `fulfillment_queue.productization_draft_markdown` 추가: 저장된 운영 메모의 누적 micro-itch 병목을 읽어 상위 1~3개 job별 `narrow pain-offer` 초안을 만든다. 각 초안에는 기준 기간, 누적 수요 신호, hero headline, promise, deliverable fields, 첫 intake 질문, CTA(`이 반복 업무 줄이기 파일럿 신청`), scope guard를 포함한다. `save_productization_draft`와 CLI `python3 src/fulfillment_queue.py productize` 추가: 기본 저장 위치는 `data/fulfillment_reports/productization-YYYY-MM-DD.md`. 같은 폴더에 저장돼도 `weekly_summary`가 제품화 초안을 운영 메모로 오인하지 않도록 `memo_snapshots`는 실제 `# pain 파일럿 운영 메모 -` 파일만 읽게 보강. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 테스트 보강: 제품화 Markdown 내용, 저장 결과, 초안 파일이 주간 집계에서 제외되는지 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 생성된 제품화 초안을 실제 `/pain-offer` 랜딩 HTML의 headline/promise/CTA에 자동 반영.
- **P35 (주간 병목 기반 제품화 우선순위 3개 추천) ✅:** P34의 주간 요약이 병목 변화만 보여주던 상태에서, 다음 주 무엇을 제품화할지 바로 판단할 수 있게 확장. `fulfillment_queue.productization_priorities` 추가: `top_micro_itches`의 `job::micro-itch` 누적 카운트를 demand_count로 보고 상위 3개를 추천한다. 각 추천에는 job, micro-itch, demand_count, 산출물 위치, 필수 칸, 첫 확인 질문, 다음 제품화 행동(`전용 pain-offer 문구, 샘플 이행서, 납품 표 첫 섹션을 이 micro-itch 중심으로 고정`)을 포함. `weekly_summary` 반환값에 `productization_priorities`를 추가하고, `weekly_summary_markdown`에 `## 다음 주 제품화 우선순위` 섹션을 노출. 테스트 보강: 주간 메모 2개 기준 top micro-itch가 rank 1 제품화 후보가 되고, 산출물 위치/필수 칸/제품화 행동이 포함되는지 검증. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 우선순위 1위 micro-itch로 자동 생성되는 좁은 랜딩/오퍼 카피 초안을 별도 Markdown으로 저장.
- **P34 (저장된 운영 메모 기반 주간 병목 요약) ✅:** P33에서 날짜별 Markdown 메모를 저장할 수 있게 된 뒤, 여러 메모를 다시 읽어 추세를 볼 수 있게 확장. `fulfillment_queue.memo_snapshots`는 `data/fulfillment_reports/*.md`를 읽어 날짜, total/open/overdue/due_today, `micro-itch별 open` 섹션을 파싱한다. `weekly_summary`는 최근 N개 메모 기준 open 변화, overdue 변화, 최신 큐 상태, 누적 micro-itch 병목을 계산하고, `weekly_summary_markdown`은 이를 Markdown으로 출력한다. CLI `python3 src/fulfillment_queue.py weekly --days 7` 추가. 테스트 보강: 2일치 운영 메모를 만들어 open/overdue delta와 누적 micro-itch 병목 합산, Markdown 출력 검증. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 주간 요약을 바탕으로 "다음 주 제품화 우선순위 3개"를 자동 추천.
- **P33 (큐 운영 리포트 Markdown 메모 저장) ✅:** P32까지는 콘솔에서 병목·산출물 칸·첫 확인 질문을 볼 수 있었지만, 매일/매주 운영 흔적이 남지 않는 문제를 보강. `store.FULFILLMENT_REPORT_DIR=data/fulfillment_reports` 추가 및 `.gitignore` 보호. `fulfillment_queue.report_markdown`은 status/job/pain/micro-itch 병목, `micro_actions`, 다음 처리 작업을 Markdown 운영 메모로 렌더링하고, `save_report_memo`는 기본 `data/fulfillment_reports/YYYY-MM-DD.md`에 원자적으로 저장한다. CLI `python3 src/fulfillment_queue.py memo` 추가, `--out`/`--now`/`--limit` 지원. 메모에는 큐 상황 요약이 포함될 수 있어 외부공유·커밋 금지 문구를 포함. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 테스트 보강: Markdown 내용, 운영 액션/첫 질문 포함, 파일 저장 결과가 렌더링과 동일한지 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 저장된 운영 메모를 기반으로 주간 병목 변화 요약 또는 완료율 추세를 생성.
- **P32 (큐 리포트 첫 확인 질문 추가) ✅:** P31의 `micro_actions`가 산출물 위치와 필수 칸은 알려주지만, 운영자가 고객에게 바로 물어볼 첫 문장은 다시 이행서를 열어야 했던 문제를 보강. `fulfillment_queue._micro_action_rows`가 `pain_probe.fulfillment_focus(job, [micro_itch])`를 재사용해 `first_question_ko`를 함께 반환하도록 추가. CLI `report`의 `다음 운영 액션` 섹션도 `ask:` 줄을 출력해, 병목 micro-itch별로 "마지막으로 터진 실제 상황, 원문 자료, 마감"을 바로 요청할 수 있게 했다. 테스트 보강: top micro-action에 선택 micro-itch 원문과 "마지막으로 터진 실제 상황"이 들어간 첫 질문이 포함되는지 검증. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 큐 리포트를 Markdown 운영 메모로 저장하는 명령을 추가해 매일/매주 실행 결과를 남기기.
- **P31 (micro-itch 병목 → 다음 운영 액션 연결) ✅:** 큐 리포트가 `by_micro_itch` 카운트만 보여주면 운영자가 다시 이행서를 열어야 했던 문제를 줄임. `fulfillment_queue.operational_report`에 `micro_actions` 추가: open 작업의 상위 micro-itch를 집계한 뒤 `pain_probe.artifact_adjustment`로 산출물 위치, 필수 칸, QA 기준을 붙인다. CLI `python3 src/fulfillment_queue.py report`에는 `다음 운영 액션` 섹션을 추가해 예를 들어 `video-editor · 수정 체크리스트 상단`과 `fields: source / timecode_or_scene / original_request / normalized_request / status`를 바로 보여준다. 완료된 작업은 기존처럼 병목과 액션에서 제외된다. `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 테스트 보강: operational_report가 top micro-itch별 `micro_actions`를 올바른 전용 산출물 필드와 함께 반환하는지 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 운영자가 큐에서 바로 "첫 확인 질문"까지 복사할 수 있도록 micro_actions에 follow-up 질문을 붙이기.
- **P30 (15개 직업군 대표 micro-itch 전용 산출물 규칙 완성) ✅:** P29에서 6개 직업군을 우선 구체화한 뒤, 남은 총무, 콜센터, 교사, 간호사, 번역가, 디자이너, HR, 기자, 법률사무원까지 대표 micro-itch 전용 산출물 칸을 추가. 예: 총무 요청 추적 → `request / department / owner / deadline / blocker / reminder_text`, 콜센터 후처리 → `call_reason / customer_emotion / confirmed_fact / promised_action / after_call_task`, 교사 수준별 자료 → `level / learning_goal / activity / scaffold / check_question`, 간호사 차팅 → `observation / intervention / patient_response / report_status / missing_check`, 번역 MTPE → `segment_id / source_text / mt_output / risk_type / fix_priority / reviewer_note`, 디자이너 브리프 → `brief_word / possible_meaning / clarifying_question / decision_needed / design_risk`, HR 후보 요약 → `candidate / jd_requirement / resume_evidence / concern / interview_question`, 기자 보도자료 선별 → `release_title / news_value / affected_party / fact_to_check / reporting_next_step`, 법률사무원 타임라인 → `date / actor / event / evidence_file / issue_tag / attorney_question`. 이제 15개 직업군 대표 작은 가려움은 모두 전용 칸/QA 기준으로 내려가며, 나머지 선택값은 공통 안전 표로 fallback. `web/sample-fulfillment-catalog.md`, `DEPLOY.md` 갱신. 테스트 보강: 남은 9개 직업군 artifact_adjustment 필드 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 이 전용 산출물 규칙을 오퍼/이행서뿐 아니라 큐 리포트의 "다음 운영자가 바로 할 일" 추천으로 끌어올리기.
- **P29 (주요 직업군 micro-itch 산출물 규칙 세분화) ✅:** P28에서 후보 풀이 넓어진 뒤, 선택된 가려움이 generic 표로 떨어지는 문제를 줄이기 위해 `pain_probe._artifact_hint`에 직업군별 전용 산출물 칸을 확장. 기존 영상편집자/마케터 외에 주니어 개발자, 영업, 데이터 분석가, 회계/세무 규칙을 추가했다. 예: 주니어 개발자 "어느 파일부터 봐야 할지 모름" → `entrypoint / likely_file / reason / unknown / first_probe_command`, 영업 미팅 준비 → `account_signal / stakeholder / likely_pain / proof_point / opening_question`, 데이터 분석 지표 급락 → `metric / numerator / denominator / comparison_window / first_check_sql`, 회계 누락자료 → `client / missing_doc / period / deadline / request_sentence / received_status`. 각 규칙은 확정 판단 대신 확인 질문·상태값을 남기도록 QA 기준을 포함. 주니어 개발자 샘플 입력에 micro-itch를 넣어 `web/sample-fulfillment-junior-developer-unknown-codebase-context.md`에 `micro-itch 산출물 조정` 블록이 나오게 갱신. `web/sample-fulfillment-catalog.md`, `DEPLOY.md` 갱신. 테스트 보강: 4개 직업군 artifact_adjustment 전용 필드, 주니어 개발자 샘플 이행서 조정 블록 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 나머지 직업군(콜센터, 교사, 간호사, 번역가, 디자이너, HR, 기자, 법률사무원, 총무)도 전용 산출물 규칙으로 확장.
- **P28 (직업별 micro-itch 후보 10개 이상 확장) ✅:** 추천 루프가 생겼지만 후보 풀이 얕으면 추천도 얕아지는 문제를 보강. `src/pain_probe.py`의 15개 직업군 micro-itch를 각 8개 수준에서 최소 10개 이상으로 확장했다. 예: 주니어 개발자는 배포 전 env/feature flag/마이그레이션 체크와 리뷰 코멘트 범위 확산, 영상편집자는 원본 소스/폰트/로고 경로 재확인과 수정본 변경점 설명문, 마케터는 UTM/캠페인명 혼선과 후기 소재화 시 허위·과장 경계 등을 추가. `/pain` 온보딩은 후보를 8개까지 보여주고 저장은 기존처럼 최대 6개로 제한해 선택지는 넓히되 운영 복잡도는 유지. `validate_against_jobs`와 테스트 기준을 `micro_itches_ko >= 10`으로 올려 퇴행을 막음. 샘플 `web/sample-pain-video-editor.html`, `web/sample-pain-probes.md` 재생성, `DEPLOY.md` 갱신. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 확장된 micro-itch별 산출물 조정 규칙을 영상/마케팅 외 주요 직업군에도 더 구체화.
- **P27 (선택 빈도 기반 micro-itch 기본 추천) ✅:** 작은 가려움을 보여주고 저장하는 데서 한 걸음 더 나아가, 실제 선택 데이터가 다음 사용자 경험을 좁히도록 연결. `pain_intents.recommended_micro_itches` 추가: 저장된 pain intent를 aggregate로 집계하고, atlas에 있는 문장만 추천하며, 선택한 pain에 데이터가 없으면 같은 직업군 전체 상위 micro-itch로 fallback. 서버는 `/pain` 렌더링 시 추천값을 넘겨 해당 체크박스를 기본 체크하고 `많이 선택됨` 배지를 표시하며, `/pain-offer`는 URL에 `mi=`가 없을 때 추천 micro-itch로 자동 좁혀서 "선택 때문에 달라지는 결과물"을 보여준다. 연락처/상황 원문은 추천에 노출하지 않는다. 샘플 `web/sample-pain-video-editor.html`, `web/sample-pain-offer-video-editor.html` 재생성, `DEPLOY.md` 갱신. 테스트 보강: 추천 helper의 pain별/직업군 fallback, 서버 추천 helper, 조작 문장 미노출, 온보딩 precheck 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 15개 직업군의 micro-itch 수를 더 촘촘히 늘려서 추천 후보 자체를 더 날카롭게 만들기.
- **P26 (micro-itch 선택 기반 오퍼 산출물 좁히기) ✅:** 사용자가 작은 가려움을 체크해도 오퍼가 단순 리스트 노출에 머물던 문제를 보강. `report.pain_offer_html`이 `pain_probe.selected_micro_itches`로 atlas 밖 문장을 다시 필터링하고, 유효한 선택값이 있으면 hero 문구를 첫 micro-itch 중심으로 좁힘. 또한 `pain_probe.artifact_adjustment`를 오퍼 페이지에도 연결해 "선택 때문에 달라지는 결과물" 섹션을 노출: 산출물 위치, 필수 칸/행, QA 기준을 결제/사전신청 전에 보여준다. 영상편집자 수정충돌 선택 시 `conflict_group / request_a / request_b / decision_needed / proposed_reply` 같은 납품 칸이 오퍼에서 바로 보이고, 조작된 micro-itch 문장은 오퍼에 표시하지 않는다. 샘플 `web/sample-pain-offer-video-editor.html` 재생성, `DEPLOY.md` 갱신. 테스트 보강: 오퍼의 선택 기반 결과물 조정 섹션, 필수 칸 노출, 조작 문장 미노출 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 직업군별 micro-itch를 더 촘촘히 늘리고, 선택 빈도 상위 micro-itch를 랜딩/CTA 기본값으로 자동 추천.
- **P25 (micro-itch 운영 병목 리포트) ✅:** P24에서 산출물 칸까지 내려간 작은 가려움을 운영 큐의 병목 지표로 승격. `fulfillment_queue.import`가 pain intent의 `micro_itches`를 작업 큐에 보존하고, `list/next` 요약 행은 첫 micro-itch를 함께 표시. `operational_report`는 open 작업만 기준으로 `by_micro_itch`를 집계해 `video-editor::수정 요청이...`처럼 직업군별 작은 가려움 병목을 보여주며, `report` 출력에도 `micro-itch별 open` 섹션을 추가. 완료된 작업은 병목 집계에서 제외해 지금 막혀 있는 가려움만 보이게 함. 문서 `DEPLOY.md`, `web/sample-fulfillment-catalog.md` 갱신. 테스트 보강: 큐 import 시 micro-itch 보존, 이행서 조정 블록 렌더, open 기준 micro-itch 병목 집계 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** micro-itch 병목 상위 1개를 전용 오퍼 카피/샘플 산출물/랜딩 CTA에 자동 반영.
- **P24 (micro-itch별 산출물 표/행 자동 조정) ✅:** P23의 다음 단계인 "micro-itch별 산출물 표 컬럼/템플릿 행까지 자동 조정"을 구현. `pain_probe.artifact_adjustment` 추가: 선택된 작은 가려움을 atlas로 검증한 뒤 `artifact_slot_ko`, `template_fields_ko`, `qa_check_ko`로 변환. 영상편집자 수정혼선 예시에서는 "요청이 여러 채널에 흩어짐" → `source / timecode_or_scene / original_request / normalized_request / status`, "요청끼리 충돌" → `conflict_group / request_a / request_b / decision_needed / proposed_reply`로 산출물 칸을 강제. `fulfillment.generate`는 `## micro-itch 산출물 조정` 표를 추가해 운영자가 납품 표에서 어떤 필수 칸/행과 QA 기준을 넣어야 하는지 바로 확인하게 함. 일반 직업군도 자료 누락, 기록/요약, 질문 설계, 범위/추가비, 설명문 유형에 맞춰 기본 산출물 칸을 생성. 샘플 `web/sample-fulfillment-video-editor-revision-chaos.md` 갱신. 테스트 추가: artifact_adjustment helper, 영상편집자/마케터 이행서 조정 표 검증. 검증: `python3 tests/test_core.py` 69개 통과, `git diff --check` 통과. **다음:** micro-itch 선택값을 큐 운영 리포트에도 노출해 어떤 작은 가려움이 병목인지 볼 수 있게 하기.
- **P23 (micro-itch 기반 이행서 우선순위·확인 질문) ✅:** 선택된 작은 가려움이 오퍼 카피에서 끝나지 않고 실제 납품 문서의 작업 순서까지 내려가게 연결. `pain_probe.selected_micro_itches`와 `pain_probe.fulfillment_focus` 추가: atlas 밖 문장은 버리고, 선택된 micro-itch를 `priority_focus_ko`, 운영자 반영 규칙, 추가 확인 질문, 넘지 말아야 할 경계로 변환. `fulfillment.generate/build_from_intent`는 선택 micro-itch가 있으면 `## micro-itch 우선순위` 블록을 생성해 작업 초점, 원문/버전/마감 확인, 확정 표현 금지, follow-up 질문을 표시. 영상편집자 샘플 이행서도 선택 micro-itch 2개 기준으로 갱신. 테스트 추가: atlas 밖 문장 제거, focus 생성, 이행서 우선순위/질문 노출 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** micro-itch별 산출물 표 컬럼/템플릿 행까지 자동 조정.
- **P22 (micro-itch 기반 파일럿 오퍼 좁히기) ✅:** micro-itch가 저장·랭킹되는 데서 끝나지 않고 오퍼 전환 문맥까지 이어지게 만듦. `/pain` 온보딩의 체크박스에 `data-mi` 인덱스를 부여하고 선택 변경 시 `파일럿 결과물 보기` 링크를 `/pain-offer?...&mi=1&mi=2`로 갱신. 서버는 `mi` query를 신뢰하지 않고 `pain_probe` atlas의 해당 직업군 인덱스로 복원해 유효한 문장만 `report.pain_offer_html`에 전달. `/pain-offer`는 "이번 파일럿에서 먼저 줄일 작은 가려움" 박스를 노출하고, 사전신청 payload에도 같은 `micro_itches`를 포함해 저장 흐름과 결박. 조작된 인덱스/범위 밖 값은 무시. `web/sample-pain-offer-video-editor.html`도 선택 micro-itch 2개가 보이는 형태로 갱신. 테스트 추가: intake 링크 갱신 JS, offer micro-itch 노출, 서버 인덱스 복원 검증. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** micro-itch 상위 항목별로 파일럿 산출물 섹션 순서나 확인 질문까지 자동 재정렬.
- **P21 (micro-itch 선택 신호 저장·랭킹) ✅:** P20에서 보여주던 작은 가려움이 단순 안내 문구로 끝나지 않게 실제 수요 신호로 연결. `/pain` 온보딩의 "혹시 이런 순간인가요?" 항목을 체크박스로 바꾸고 제출 payload에 `micro_itches` 포함. 서버 `handle_pain_intent`는 클라이언트가 보낸 문장을 그대로 믿지 않고 `src/pain_probe.py`의 직업군별 허용 목록과 대조해 최대 6개만 저장, atlas 밖 조작 문장은 버림. `src/pain_intents.py`에 `ranked_micro_itches` 추가: 대표 pain 우선순위와 별도로 작은 가려움 상위 항목, 고유 연락처 수, 연결 pain을 출력하고 CSV에도 `micro_itches` 컬럼을 포함. `fulfillment.build_from_intent` 이행서에도 선택한 작은 가려움을 표시해 운영자가 산출물 범위를 더 빨리 좁히게 만듦. README/DEPLOY 갱신. 검증: `python3 tests/test_core.py` 69개 통과. **다음:** 실제 신청 데이터가 들어왔을 때 micro-itch 상위 1개를 전용 오퍼 카피/샘플 산출물로 자동 반영.
- **P20 (직업별 micro-itch probe atlas) ✅:** 사용자의 "각 직업군에게 일하면서 가려운 곳이 엄청 많다"는 지점을 대표 pain보다 한 단계 더 쪼갬. `src/pain_probe.py` 추가: 15개 직업군마다 최소 8개의 작은 업무 가려움, 검증 질문, 돈 내는 순간, 좁은 wedge, 하지 말아야 할 경계를 구조화. `/pain` 온보딩에 "혹시 이런 순간인가요?" 패널을 붙여 영상편집자라면 수정 요청 충돌/타임코드 없음/자막 표기/추가비 같은 micro-itch를 바로 보여주고, 사용자가 상황 설명을 더 구체적으로 쓰게 만듦. 운영 문서 `web/sample-pain-probes.md` 생성, README/DEPLOY에 `python3 src/pain_probe.py --out web/sample-pain-probes.md` 연결. 테스트 추가: 모든 직업군 probe atlas 커버리지, micro-itch/질문 수, 온보딩 노출 검증. 검증: `python3 tests/test_core.py` 69개 통과, `git diff --check` 통과. **다음:** 실제 pain intent가 쌓이면 micro-itch별 선택 빈도까지 저장해 오퍼 문구를 자동으로 좁히기.
- **P19 (런칭 모드별 통합 preflight) ✅:** 개별 법무/결제/PII 체크가 흩어져 있어 실제 오픈 직전 실수 가능성이 남아 있던 문제를 막음. `src/launch_preflight.py` 추가: `--mode lead|paid|pain-paid` 지원. 공통 체크는 `legal_preflight.check()` 통과, `GEMINI_API_KEY`, `REPORT_BASE_URL=https`, `WEBHOOK_TOKEN`, `INTEREST_SALT`, PII 런타임 파일 .gitignore 보호. `lead` 모드는 `PAYMENT_URL`/`PAIN_PAYMENT_URL`이 켜져 있으면 실패해 실수 결제 노출을 차단. `paid` 모드는 `PAYMENT_URL=https`, `PAYMENT_WEBHOOK_SECRET` 16자 이상, `PAYMENT_EXPECTED_AMOUNT` 양수, `PAYMENT_ALLOWED_AMOUNTS` 포함 여부를 검증. `pain-paid`도 `PAIN_PAYMENT_URL`, `PAYMENT_WEBHOOK_SECRET`, `PAIN_PAYMENT_EXPECTED_AMOUNT`를 같은 방식으로 검증. README/DEPLOY에 `python3 src/launch_preflight.py --mode lead|paid|pain-paid`를 런칭 전 필수 명령으로 연결. 테스트 추가: 미준비 리드 실패, 준비된 리드 통과, 리드 모드 결제 URL 차단, paid/pain-paid 결제 가드 통과. 검증: `python3 tests/test_core.py` 68개 통과. **다음:** 실제 운영 env 예시 파일 템플릿과 시크릿 파일 권한 점검.
- **P18 (LEGAL preflight 배포 차단) ✅:** P17에서 만든 `/privacy`·`/terms` 초안이 실제 사업자 정보 없이 배포되는 걸 막기 위해 `src/legal_preflight.py` 추가. 필수 `LEGAL_*` env 10개(`LEGAL_OPERATOR_NAME`, `LEGAL_OPERATOR_ADDRESS`, `LEGAL_CONTACT_EMAIL`, `LEGAL_PRIVACY_OFFICER`, `LEGAL_BUSINESS_NUMBER`, `LEGAL_TELECOMMERCE_NUMBER`, `LEGAL_PAYMENT_PROCESSOR`, `LEGAL_HOSTING_PROVIDER`, `LEGAL_NOTIFICATION_PROVIDER`, `LEGAL_FULFILLMENT_FIELDS`)가 비었거나 렌더링된 방침/약관에 `[필수 입력]`이 남으면 실패. 기본 로컬 상태에서는 의도적으로 실패하고, 모든 LEGAL 값을 채우면 `legal preflight ok`로 통과. README/DEPLOY에 `python3 src/legal_preflight.py`를 PII 게이트 명령으로 기록. 테스트 추가: placeholder 미입력 시 실패, env 채움 시 통과. 검증: 기본 실패 경로 확인, env 채움 통과 확인, `python3 tests/test_core.py` 65개 통과, `git diff --check` 통과. **다음:** 실제 결제/리드 오픈 전 operator env 파일 예시와 시크릿/PII 운영 preflight 묶음.
- **P17 (PII 게이트: 개인정보처리방침·약관 초안 라우트) ✅:** 리드/파일럿/결제 전에 막혀 있던 법무 신뢰 표면을 실제 서비스 라우트로 연결. `report.privacy_html()`·`report.terms_html()` 추가, server GET `/privacy`·`/terms` 연결, 랜딩/리포트/상세/오퍼/결제접수 페이지 하단에 법무 링크 노출. `/offer`, `/pain`, `/pain-offer` 동의 문구에 개인정보처리방침/이용약관 링크를 직접 연결. 방침 초안에는 처리목적, 수집항목, 보유기간, 제3자 제공, 처리위탁, 정보주체 권리, 파기, 안전성 확보, 자동화된 결정/전문판단 제외, 문의 채널을 포함. 약관 초안에는 사업자 정보, 상품/가격/제공시점, 청약철회·취소·환불, 전문 판단 제외, 금지행위, 개인정보처리방침 연결을 포함. `LEGAL_*` env로 사업자/문의/위탁 정보를 채울 수 있게 하고, [필수 입력]이 남으면 리드 수집을 켜지 말라고 DEPLOY에 명시. 샘플 `web/sample-privacy.html`, `web/sample-terms.html` 생성. 공식 출처 확인: 개인정보보호위원회 2026 처리방침 작성지침, 국가법령정보센터 개인정보 보호법/시행령, 전자상거래법 제17조 관련 법령해석례. 검증: `python3 tests/test_core.py` 63개 통과, `git diff --check` 통과, 로컬 서버 `/privacy`·`/terms` HTTP 200 확인.
- **P16 (직업별 진짜 가려움 심층 아틀라스) ✅:** 사용자의 "각 직업군에게 일하면서 가장 가려운 곳을 더 깊게" 요청을 온보딩/오퍼 구매 맥락까지 확장. `src/pain_deepdive.py` 추가: 15개 직업군 대표 pain마다 **터지는 순간, 숨어있는 비용, 그냥 AI로 부족한 이유, 최소 입력, 첫 10분 안도감, 구매 트리거, 파일럿 약속, 성공 지표, 사람 경계선**을 구조화. `/pain` 온보딩에는 "정말 가려운 지점" 패널을 붙여 사용자가 자기 상황을 더 구체적으로 쓰게 만들고, `/pain-offer`에는 "왜 돈 내고 줄이는가" 패널을 붙여 39,000원 파일럿의 구매 이유와 성공 기준을 명확히 함. 운영 문서 `web/sample-pain-atlas.md` 생성, README/DEPLOY에 갱신 명령 연결. 테스트 추가: 모든 직업군 심층 pain 존재, 실제 painmap 연결, 온보딩/오퍼 노출 검증. 61테스트 통과. **다음:** 개인정보처리방침·약관 템플릿 또는 실제 pain intent 상위 항목 기반 첫 결제 오퍼 카피 강화.
- **P15 (큐 기반 운영 리포트) ✅:** pain 파일럿 큐를 매번 눈으로 훑지 않아도 병목을 파악할 수 있게 `src/fulfillment_queue.py report` 추가. `operational_report`는 total/open/overdue/due_today, status_counts, open 기준 job/pain 병목, 다음 처리 작업을 반환하고, 출력에서는 연락처를 마스킹한 기존 요약 행만 사용. overdue 작업이 다음 처리 목록 상단에 오고, 이후 due_at이 빠른 open 작업이 정렬됨. `DEPLOY.md`와 `web/sample-fulfillment-catalog.md`에 `report` 명령 추가. 테스트 60개 통과. **다음:** 운영 리포트 기반 주간 텍스트 리포트 저장 또는 개인정보처리방침·약관 템플릿.
- **P14 (파일럿 이행 SLA/overdue 운영) ✅:** 특정 pain 파일럿이 약속한 "3영업일 내 납품"을 실제 큐 운영에 반영. `src/fulfillment_queue.py`에 `DELIVERY_SLA_BUSINESS_DAYS=3`, `add_business_days`, `is_overdue` 추가. pain intent를 큐로 import할 때 `due_at`과 `sla_business_days`를 기록하고, `list`는 due/OVERDUE 플래그를 출력하며 `next`는 단순 FIFO가 아니라 due_at이 가장 빠른 queued 작업을 반환. 완료/취소된 작업은 overdue로 보지 않음. `DEPLOY.md`와 `web/sample-fulfillment-catalog.md`에 due/overdue 운영 문구 반영. 테스트 59개 통과. **다음:** 큐 기반 주간 운영 리포트 또는 개인정보처리방침·약관 템플릿.
- **P13 (pain 이행 작업 큐) ✅:** pain intent를 바로 이행서로 만드는 P12 다음 병목인 "신청 처리 상태가 사라짐"을 막기 위해 `src/fulfillment_queue.py` 추가. 기능: `import`로 `data/pain_intent.jsonl`의 신청을 `data/fulfillment_jobs.jsonl` 큐로 승격, 중복 import 방지, `list`로 상태 요약, `next`로 다음 queued 작업 확인, `render <id>`로 해당 작업의 이행서 출력, `status <id> working/delivered/blocked/canceled --note`로 처리상태 기록. 큐 파일은 연락처/상황 PII를 포함하므로 `.gitignore`에 `data/fulfillment_jobs.jsonl` 추가. 정직성: 큐는 자동 납품/결제 증명이 아니라 사람이 직접 처리할 일감 목록이며, 이행서 출력은 연락처를 마스킹. `DEPLOY.md`와 `web/sample-fulfillment-catalog.md`에 큐 운영 명령 추가. 테스트 57개 통과. **다음:** 개인정보처리방침/약관 템플릿 또는 큐 기반 주간 운영 리포트.
- **P12 (pain intent → 이행서 운영 연결) ✅:** `/pain`에서 저장된 실제 신청을 운영자가 다시 복사하지 않고 바로 납품 문서로 바꿀 수 있게 `src/fulfillment.py`를 확장. `intent_rows`, `select_intent`, `build_from_intent` 추가, CLI `python3 src/fulfillment.py --from-intent latest` 지원. `--job`, `--pain` 필터로 특정 직무/고통의 최근 신청만 선택 가능하고, selector는 `latest`, `1`, `-1` 등으로 지정. 연락처는 출력에서 마스킹하고, 잘못된 selector/필터는 `ValueError`로 실패해 엉뚱한 이행서 생성을 막음. `DEPLOY.md`와 `web/sample-fulfillment-catalog.md`에 실제 신청 처리 명령 추가. 테스트 55개 통과. **다음:** 개인정보처리방침·약관 템플릿 또는 이행서 결과물을 운영자가 체크/상태관리하는 큐 추가.
- **P11 (pain 파일럿 이행 템플릿 고정) ✅:** 사용자의 "각 직업군이 일하면서 진짜 가려운 곳" 요청을 결제 가능한 운영 단위까지 더 내림. `src/fulfillment.py` 추가: `python3 src/fulfillment.py --job video-editor --pain revision-chaos --sample`로 특정 직업군/pain의 컨시어지 이행서 Markdown 생성. 이행서 구성: 신청 요약, 약속 범위, 수집 자료, 운영자 체크리스트, 납품 골격, 금지/가드레일. 15개 직업군 대표 pain 모두 전용 템플릿 적용: 영상편집자 **타임코드별 수정 체크리스트**, 개발자 **수정 영향도 맵+PR 설명문**, 마케터 **주간 성과 해석+다음 실험**, 영업 **3분 미팅 브리프+발견 질문**, 데이터분석가 **원인 후보 트리+SQL 초안**, 회계/세무 **누락자료 체크리스트+고객 안내문**, 총무 **요청 추적 보드+리마인드**, 디자이너 **수정 범위표+추가비 안내**, 교사 **수준별 활동지+채점 루브릭**, 간호사 **차팅 문장+누락 확인**, 번역가 **번역 QA 리포트**, 기자 **보도자료 선별표**, HR **후보자 요약표**, 콜센터 **상담 요약+이관 메모**, 법률사무원 **사건 타임라인+증거목록**. 정직성: 연락처 마스킹, 전문 판단/성과 보장 금지, 개인정보 최소사용, 사람이 최종 검토. 샘플 `web/sample-fulfillment-video-editor-revision-chaos.md`와 카탈로그 `web/sample-fulfillment-catalog.md` 생성, DEPLOY 체크리스트에 결제 오픈 전 이행서 생성 명령 추가, 테스트 53개 통과. **다음:** 개인정보처리방침/약관 템플릿 또는 실제 pain intent 상위 항목 기반 오퍼 카피 강화.
- **P10 (pain별 좁은 파일럿 오퍼 + 다중 결제금액 검증) ✅:** pain intent 상위 문제를 바로 검증할 수 있도록 범용 99,000원 커리어 패키지와 별도인 **특정 업무 고통 1개짜리 파일럿 오퍼**를 추가. `report.pain_offer_html` + server `GET /pain-offer?job=&pain=` 연결, `/pain` 온보딩에서 파일럿 결과물 보기 CTA 추가. 기본 가격 `PAIN_OFFER_PRICE=39,000`, 배송 3영업일, 실결제는 `PAIN_PAYMENT_URL` 설정 시에만 활성화(미설정 시 파일럿 사전신청). 정직성: "사전신청은 결제가 아님", "컨시어지 검증", "자동화된 전문 판단/성과 보장 없음" 고지. 결제 보안: 기존 단일 `PAYMENT_EXPECTED_AMOUNT` 구조를 보강해 `PAIN_PAYMENT_EXPECTED_AMOUNT` 및 `PAYMENT_ALLOWED_AMOUNTS=99000,39000` 명시 목록을 지원, 서명검증 후에도 허용 금액만 `paid`로 인정. `pain_intents`에 `offer_type=pain-pack` 저장 가능, 운영 CLI CSV에도 `offer_type` 노출. 샘플 `web/sample-pain-offer-video-editor.html` 생성, 테스트 49개 통과. **다음:** 실제 결제/파일럿 전 개인정보처리방침·이행 템플릿 고정 또는 1차 타깃 전용 카피/샘플 산출물 강화.
- **P9 (pain intent 운영 대시/CLI) ✅:** `/pain`에서 모은 "어떤 업무 고통을 해결하고 싶은가" 신호를 운영자가 바로 읽을 수 있게 `src/pain_intents.py` 추가. 기능: 총 pain intents, 제품화 우선순위(건수·고유 연락처·샘플 제공 가능성·구체 상황 기반), 직무별/샘플가능성별 분포, 최근 10건, CSV 내보내기(`--csv`, `--raw`). 기본 출력은 연락처 마스킹, CSV 수식 인젝션 방어, `--raw`만 평문 경고. `DEPLOY.md` §2.6에 `pain_intents` 측정 명령과 라벨 정직성(지불/리드 아님, 제품화 우선순위 신호) 추가, README 한 줄 흐름도 "진짜 가려운 업무 선택" 반영. 테스트 48개 통과. **다음:** pain intent 상위 1개 직업/고통을 기준으로 전용 랜딩/결과물 샘플/실결제 오퍼를 좁히기.
- **P8 (가려움 기반 온보딩 + 수요신호 분리) ✅:** P7 pain map을 "읽는 카드"에서 **선택 가능한 검증 퍼널**로 확장. 리포트의 각 "진짜 가려운 업무" 카드에 `/pain?job=...&pain=...` 링크 추가 → 전용 온보딩 페이지 `report.pain_intake_html`에서 사용자가 현재 일하는 형태, 실제 상황, 샘플 제공 가능 여부, 연락처를 남김. `server.py`에 GET `/pain` + POST `/pain/intent` 연결, `store.py`에 `pain_intent.jsonl` 별도 저장/중복제거(contact+job+pain) 추가. `/health`에 `pain_intents` 노출하되 **지불/리드와 분리**(pain_intents=어떤 업무 고통을 제품화할지 검증하는 신호, presale_leads≠paid_customers). 정직성: 페이지에 "결제가 아님" 명시, pain map은 제품 가설 라벨 유지, 전문 판단 자동화 금지 고지. 보안/개인정보: consent 필수, contact 검증, honeypot, IP는 HMAC(솔트 없으면 미저장), `data/pain_intent.jsonl` .gitignore 추가. 테스트 46개 통과, `web/sample-pain-video-editor.html` 생성.
- **P7 (직업별 '가려움 지도' 제품화) ✅:** 사용자 요청="각 직업군에게 정말 일하면서 가장 가려운 곳을 더 깊게"에 맞춰 **AI 대체 위험 → 현업 반복 고통 → 손에 남는 산출물**로 한 단계 더 내려감. `src/painmap.py` 신규 추가: 15개 직업군 전체에 대해 pain_id·연결 task_ids·가려운 순간·서비스가 해줄 일·산출물·심각도/빈도/WTP 가설을 구조화. `report.render_html`에 **"진짜 가려운 업무"** 섹션 연결 — 각 직업 리포트가 단순 압력지수가 아니라 "이 사람이 매주 겪는 짜증을 무엇으로 줄여줄지"까지 보여줌. 정직성 가드레일: pain map은 사용자 인터뷰 전 **제품 가설**로 라벨링, "유료 판단 근거가 아니라 문제 발견용" 문구 노출(가짜 검증/가짜 수요 금지). 테스트 추가: 모든 직업의 pain map 존재, task_id 연결 무결성, report 노출 확인. 43테스트 통과. **다음:** 실제 1차 타깃(영상편집자/마케터/영업/데이터분석/주니어개발자) 중 하나를 골라 pain map 기반 랜딩·온보딩 질문·결과물 패키지를 좁게 선판매.
- **R1 (설계+검증):** 루브릭 v1 작성 → Gemini(카피/UX)+Codex(적대 리뷰) 실호출 검증 → **v2 피벗 완료**(태스크 단위·근거등급·신뢰구간·기상예보·다음행동·한국규제 가드레일). 셋 다 "단일 숫자 격하"에 수렴.
- **R2 (코드+검증) ✅:** v2 루브릭을 코드화. `src/scoring.py`(점수엔진, 실행됨)·`src/crawler.py`(수집 골격)·`data/jobs/{video-editor,junior-developer}.json`(시드 직무-태스크 매트릭스). Codex 적대 리뷰로 치명버그 다수 수정: 밴드 경계 빈틈, 요인 음수 부호뒤집힘, 출처불명→Δ=0, 멀티태스크 dedup 충돌(키에 job/task/direction 포함), daily cap 미작동·폭주(날짜별·상승/하락 버킷·부분 클램프), 비결정적 순서(Tier·|Δ| 정렬). 데모: 영상편집자 59[흐림]/주니어개발자 50.5[구름조금].
  - **[자율결정]** R2는 백엔드라 Codex 중심 검증, Gemini(카피/UX)는 R4(카카오봇 카피·UI)에서 투입.
- **R3a (구현+라이브검증) ✅:** `src/store.py`(이벤트 스토어 + 점수로그 time series, 직전대비 델타) · `src/gemini_scorer.py`(Gemini 2.5 Pro 5요인 점수화, 키는 env GEMINI_API_KEY, responseMimeType=json + 자체검증) · `src/crawler.py` RSSSource(stdlib urllib+xml, RSS/Atom). **라이브 테스트 통과:** store 시계열·델타 / TechCrunch 실수집 3건 / Gemini가 영향없는 업무 정확히 제외하고 5요인 JSON 반환. 런타임 생성물(data/events, data/scores)은 .gitignore.
  - **[자율결정]** R3 분량 커서 R3a(모듈+검증)/R3b(배선+통계)로 분할.
- **R3b-1 (파이프라인 배선+라이브검증) ✅:** `src/pipeline.py` — fetch→관련성게이트→Gemini 점수화→Event저장→**전체 히스토리 감쇠 재점수(=mean-reversion ③)**→점수로그 append. 라이브 통과(OpenAI 블로그 'Endava' 기사 실채점, 멱등성 OK). **[자율결정+라이브 결함수정]** 벤더 자기홍보 블로그(openai/google/anthropic)를 tier1→**tier3(PR, max+2)** 강등 — 실데이터에서 벤더PR이 adoption/scale 펌핑하는 것 포착, 주니어지수 정정.
- **R3b-2-a (수집/저장 정합성) ✅:** ①원자적 Event 저장(`status` complete/irrelevant/failed, 부분실패는 재시도, recompute는 complete만 반영) ②구조화 dedup_key(Gemini가 technology/vendor 반환→affected별 키, 논문→데모→GA 클러스터) ③_event_id 강건화(추적파라미터만 제거하는 canonical URL — Codex가 ?id 충돌 지적해 정밀화) ④조건부 스냅샷 append(index≥0.1·날씨 변화 시만). 라이브+Codex 교차검증 통과(Codex fix-first 2건 반영: URL충돌·affected별 키).
- **R3b-2-b (점수엔진 정확도) ✅:** ①**factor별 신뢰도 분리**(FACTOR_CONF[tier][factor] — 벤더 maturity 0.9 신뢰/adoption·scale 0.3 보수, 전역 신뢰도 1개를 factor별로 대체) ②**Tier3 누적 캡**(TIER3_JOB_CAP=6/직업, 대량 PR 펌핑 차단) ③**드라이버 표시 프루닝**(점수 누적엔 전부 반영 — Codex가 '작은신호 다수 소멸' 지적해 표시용으로만 제한). 라이브+단위테스트+Codex fix-first 통과(작은신호5건→컷편집 70→72.3 누적 확인).
- **R3b-2-c (task-first + override) ✅:** ④`headline_task`로 태스크 우선 노출, 직업 index는 secondary 명시(소비자 무손상). ⑤Event.override 필드 + `store.save_override()`(override=True + 별도 감사로그 `overrides_audit.jsonl`)로 운영자 보정 구조 분리. 회귀 통과.
- **R4a (카카오 푸시 카피 생성기) ✅:** `src/notify.py` — 점수 스냅샷→푸시 1줄. Gemini 2.5 Pro 카피 + 가드레일 후처리(금칙어 '대체된다' 등 차단) + Gemini 없이도 동작하는 폴백 템플릿. 라이브 통과(업무우선+근거+단정없음+행동유도). 한계: 금칙어 리스트는 백스톱일 뿐 의미적 조작 전부는 못 잡음.
- **R4b (미니웹 결과리포트) ✅:** `src/report.py` — scoring 결과→모바일 우선 정적 HTML. 전략가 타입(Gemini, 위협+기회 동시 → 공유 가능, 예: "AI 편집 설계자형🧠") + 기상예보 시각화 + 태스크별 압력바 + 근거뉴스(출처 배지+링크) + 유료트리거. 라이브+Codex fix-first 통과(XSS scheme 차단 _safe_url). 샘플: `web/sample-report.html`(모바일에서 열어보면 실제 UI).
- **R4c (봇 서버 골격) ✅:** `src/server.py`(stdlib http.server, 의존성0) — POST /webhook/kakao(발화→직업선택→매핑저장→task-first 요약+푸시카피), GET /report(결과리포트 서빙), /health. store에 사용자-직업 매핑(락+원자적쓰기). 라이브 통과(엔드투엔드 사용자여정). Codex 보안 fix-first 5종: Content-Length 제한(64KB·413), body 타입검증(배열/null 크래시 방지), users.json 락+os.replace, /report 점수 TTL캐시+Gemini호출 제거(0.0007s), 127.0.0.1 기본 바인딩.
  - **배포 전 필수(R5):** 웹훅 인증/서명 검증, rate limit, reverse proxy TLS.
- **R4d (일/주 배치) ✅:** `src/batch.py` — pipeline.run()(수집·재점수) → 의미있는 변동(|Δ|≥2 or 날씨변화) 직무의 구독자에게 notify 푸시 큐잉(outbox) + 전략가타입 캐시. 카카오 발송은 스텁. 라이브 통과(영상편집자 Δ+17.3→u1 큐잉). Codex fix-first: **스냅샷 ts 기반 중복 알림 방지**(set_notified/get_notified — 매 배치 같은 알림 재큐잉 스팸 차단, 2회차 재큐잉 0 확인) + 전략가 Gemini는 변동 시에만 호출. server /report는 캐시된 전략가타입 사용.
  - **★ MVP 백엔드 완성:** 크롤링→Gemini채점→지수→시계열→봇서버(웹훅·리포트)→배치알림 전 과정 실작동. 카카오 채널 연결 + 발송 API만 붙이면 런칭 가능.
- **D1 (결과리포트 디자인 고도화, Gemini 3.1 Pro 주도) ✅:** 표현층만 프리미엄 교체(콘텐츠·데이터·가드레일 문구 불변). `src/report.py` 재설계.
  - **[자율결정]** 사용자 "Antigravity로 시도" 요청 → Antigravity는 별도 IDE라 호출 불가하나, 그 기반 모델 **gemini-3.1-pro-preview**가 API로 살아있어 디자인 생성에 사용(2.5 Pro 대비 우월). ※이전에 "Gemini 3.x 없음"이라 한 건 모델목록 출력을 [:12]로 잘라본 내 오류였음 — 3.1-pro/3.5-flash 등 실재.
  - **레퍼런스(각 적용점):** Spotify Wrapped(데이터→공유 스토리, 타입을 '페르소나'로) · 16Personalities(명확한 타입 정체성) · Toss(카드 위계·여백) · Apple Health 링(수치를 위험 아닌 '상태'로, 숫자 보조화) · Apple Weather 밴드(색으로 직관) · Linear(절제된 프리미엄 다크).
  - **구현:** 히어로 카드 안에 반원 SVG 게이지(각도=index/100×180° 동적) + 4단계 기상밴드 + 신뢰구간± / 위협·기회 양면 / task-first 업무바 / 티어배지(공식·언론·벤더PR)+근거 / 유료CTA / 가드레일 푸터. Pretendard 폰트스택·word-break:keep-all 한글 최적화.
  - **Codex fix-first:** 카피 불변 위반(가드레일/섹션/CTA 문구 변경) 전량 복원 + 태풍 task바 빨강떡칠 완화(#e11d48→#fb7185). 통과: WCAG AA(5.89:1/5.25:1), 게이지 각도수학, _safe_url XSS.
- **R5-a (배포 보안) ✅:** server.py — 웹훅 토큰 인증(env WEBHOOK_TOKEN, ?token= 불일치→401) + IP rate limit(env RATE_LIMIT, 윈도우 초과→429). 라이브 통과. **[자율결정]** 고난도 단계(카피·디자인)는 Gemini 3.1 Pro, 루틴(채점·요약)은 2.5 Pro/flash로 모델 분리(비용·품질 균형).
- **R5-b (진행 중):**
  - **CI propagation(weighted) ✅:** `scoring._propagate_job_ci` — 태스크 CI→직업 CI 전파(부분상관 휴리스틱, 정식 통계CI 아님=참고지표). None/음수 가드 + 과신 방지 하한(0.75·가중평균, fear-adjacent 보수성, Codex fix-first). 데모 ±7.6~9.1.
  - **Gemini 중앙 클라이언트 ✅(비용·신뢰성):** `src/gemini_client.py` — 역할별 모델체인(premium=3.1 Pro→2.5 Pro→flash / routine=2.5 Pro→flash) + 프리뷰 deprecate 자동강등 + 429/5xx backoff + 빈응답/MAX_TOKENS soft-fail 강등 + 키 없으면 즉시실패. report·notify를 premium 티어로 연결(역할분담: 디자인·UX·카피=Gemini premium, 코드=Codex+Claude). **[사용자 비용 확인사항]** AI Studio 결제 티어/예산알림은 사용자가 콘솔에서 확인. 다음: actionplan·gemini_scorer도 클라이언트로 이관 + dead import 청소.
  - **발송 파이프라인 ✅:** `src/sender.py` — outbox queued→발송→sent/failed 마킹 + 재시도(max 3) + per-user throttle. `StubSender`(검증용)·`KakaoSender`(스텁, 발신프로필+템플릿 승인 후 연결). batch._flush_outbox가 사용. 라이브: throttle/재시도 검증 통과.
  - **이벤트 아카이브/프루닝 ✅:** `pipeline.prune_events` — 감쇠계수<0.03 non-regulation 이벤트를 events_archive로 이동(무한 재읽기 방지). 규제·최신 보존. 매 배치 실행.
- **R5-b 완료.** 백엔드+제품+발송+하드닝 전부 실작동.
- **R6 (배포, 일부 사용자 필요):** ① README 배포가이드(서버 호스팅·batch cron·env·TLS) ← 자율 가능 ② 카카오 비즈채널+발신프로필+템플릿 승인 ← **사용자 필요** ③ 호스팅(서버 띄우기)+도메인 ← 사용자 결정.
- **R6 (배포):** 서버 호스팅 + 도메인 + TLS + cron(배치) + 카카오 채널 연결(사용자 비즈계정 필요) + README 배포가이드.
- **R5 (하드닝, 이후로 미룸):** 이벤트 만료·아카이브 / CI propagation(weighted) / 비용·쿼터(batch·캐시·backoff) / notify 가드레일 의미검증 강화.
- **R4 (이후):** 카카오 채널봇 카피·UI (Gemini 투입) + 미니웹 결과리포트(전략가타입 공유) + 유료트리거.
- **V1 (수요검증 + 포지셔닝 재설계) ✅:** 사용자 질문="실제로 지갑을 여는 수요가 있나"에 deep-research 하니스(27소스→105주장→상위25 3표 적대검증)로 답. **판정: 순수 B2C 공포구독 유료수요 약함~제한적** — 불안은 크나(전세계 40%) "불안→결제" 인과주장은 검증서 기각(1-2), 위협지수는 이미 무료 commodity(amicooked.io), churn 1위='사용부족' 37%, 커리어 인텔리전스 매출은 B2B 쏠림(리멤버 70~75% B2B). `POSITIONING.md` v2 작성: 위협지수=무료미끼 격하 / 액션플랜=결제정점 / 매출베팅 B2B·정부. **Codex 적대리뷰 반영(fix-first):** ① 액션플랜 텍스트=다음 commodity → "결과물 남는 패키지(첨삭·코호트·연계)"로 ② "혼자막막한 층"=결제력 약한 역설 ③ B2B는 다른 사업(인터뷰20+LOI3 선행) ④ KDT는 매출 아니라 법적지뢰(위탁계약 모집금지)→매출라인 삭제 ⑤ **최대리스크=지불주체 부재**. **다음=만들기 동결, 30일 내 지불주체 1명 증명**(B2C 결제 선판매 권장 / B2B LOI / 훈련기관 합법계약 중 택1). 미증명 시 코드 추가 동결.
- **V2 (런칭=지불주체 테스트 표면) ✅:** 사용자 "런칭까지 자율 진행" 지시 → 검증결론(지불주체 먼저)과 런칭을 합쳐 **사전판매 오퍼를 런칭 표면에 박음**(런칭=30일 스모크테스트). `report.offer_html`(AI 대응 스프린트, 결과물 남는 패키지=Codex fix) + server `/offer`·`POST /offer/interest` + store `append_interest`(중복제거)·`interest_count`. **정직성 fix-first 2건(중요):** ① report CTA의 **검증 안 된 가짜 사회증명 "상위 5%는 이미 대응 중" 제거** → 가치 소구로 교체 ② 오퍼 **가짜 할인 취소선(14,900) 제거** → "테스트 가격, 정식가 오픈 전 확정". **버그수정:** scoring 결과에 `job_id` 누락 → `/detail`·`/offer` 링크가 빈 링크였음(기존 detail도 깨져있던 것 발견·수정). **Codex 적대리뷰 6건 fix-first:** honeypot+동의(PIPA)필수+contact형식검증+(contact,job)중복제거 / fetch `r.ok` 미확인 거짓"완료" / 결제모드vs사전모드 고지 분리 / 가짜할인 제거 / 지표 honest 라벨(`presale_leads`=리드≠지불, 실WTP는 PAYMENT_URL 결제이벤트로) / 원시IP 평문저장 금지→해시(iph). PAYMENT_URL env 연결 시 실결제 버튼 자동전환(코드변경 불필요). 30테스트 통과·라이브 E2E 통과. 미리보기 `web/sample-offer.html`. **다음(자율 계속):** 정적 배포 가능화 + 런칭 런북(사용자 5분 계정작업: 호스팅/결제링크/카카오채널) + 측정 대시.
- **P6 (캘리브레이션 어댑터 스캐폴드 + 정직성 강등, 3-AI 루프) ✅:** baseline 손추정→실데이터 결박 기반 구축. **조사:** O*NET 자체엔 자동화확률 없음 → O*NET-SOC 기반 공개연구(Felten AIOE 2021 / Eloundou GPT exposure 2024 / Frey-Osborne) 사용. **AIOE는 명시적 오픈라이선스 없음** → 외부데이터 커밋 금지. **구현:** `src/calibrate.py`(노출 CSV→percentile 앵커, SOC 자동정규화) + `data/calibration/job_soc_map.json`(15직무→SOC, confidence high/medium) + `data/calibration/README.md` + `.gitignore`(`*.csv`/`*.xlsx`). **★Codex 적대검증이 치명결함 적발 → 정직하게 강등(가짜 캘리 방지):** ① **scoring 표시점수=태스크 롤업이라 baseline.index는 fallback일 뿐** → 직업-레벨 앵커는 표시점수 미반영 → `calibrated:true` 붙이면 가짜 정밀도 → **`calibrated:false` 유지 + `baseline.index_anchor`(참고치)만 부착**으로 강등 ② **외부 raw 점수 유출**(job JSON에 저장→라이선스 위반) → raw 미저장, 파생 percentile만 ③ medium SOC(data-analyst·teacher 등) 동일취급 위험 → **기본 보류(`--apply-medium`만)** ④ percentile은 'CSV 전체 분포' 기준임을 README 경고(15개만 넣으면 왜곡) ⑤ CI 수식 오류 제거. **진짜 캘리브레이션=태스크-레벨 노출 데이터로 태스크 baseline 결박(다음 작업), 지금은 그 기반.** 40테스트 통과(calibrate 5종), 커밋된 직무 전부 calibrated:false 유지(실데이터 미적용=정직). **다음:** 태스크-레벨 노출(Eloundou) 결박 또는 사용자 실제 배포.
- **P5 (배포 준비 감사, 3-AI 루프) ✅:** README/DEPLOY를 P1~P4 변경에 맞춰 점검·갱신 + 런칭 체크리스트 정비. **Codex 적대 감사 fix-first(배포 안전·법무·정직성):** ① **결제 후 이행 절차 미고정**(결제 받고 이행 못 함 위험) → DEPLOY §2.5에 '결제 오픈 전 고정' 블록 추가(PG가 이름·이메일·order_id 수집 / 이력서 제출 흐름 / 주5건 하드캡=PG재고or수동마감) ② **PII 방침이 연락처 중심** → 결제 후 수집 이력서·포트폴리오·인터뷰까지 포함한 처리방침·별도동의 명시 ③ **통신판매업 신고 + 사업자정보 표시** 누락 → §7 체크리스트 추가 ④ 환불/청약철회 고지 위치 반복(오퍼·결제버튼·PG·계약통지, 7일/3개월/3영업일) ⑤ 보안 footgun(umask 077·chmod 600·data 웹루트밖·`*.csv`/백업 .gitignore 추가·`?token=` 로그). **정직성:** README '공포 훅'→'미끼→결과물·통제감', '규제 가드레일 내장'→'일부 제품 가드레일'(법무 전체준수 아님), 오퍼 '커리어 디렉터'→'운영자'(1인 MVP 과장 완화), 공유문구 '너의 직업은 안전할까'(공포)→'얼마나 영향받을까'. 오퍼 grounded 운영노트(배치 1회 먼저). 직군수 10→15 정정. 35테스트 통과, 시크릿 clean. **다음:** O*NET 캘리브레이션(손추정→calibrated, 신뢰성 핵심) 또는 사용자 실제 배포 액션.
- **P4 (액션플랜 프롬프트 정직성 하드닝, 3-AI 루프) ✅:** Codex가 두 번 지적한 생성 오염 정리 — `actionplan.py` `_PROMPT` 도입부의 '생존 액션플랜 엔진', "유료 '상위 5% 대응법'의 실체이므로, 남들이 모를 만큼 구체적"(가짜위계·배타성·공포 프레이밍이 LLM 생성을 오염) → **Gemini 재작성**: '직무 대응 액션플랜 엔진 … 사용자가 주도적으로 통제할 수 있는 … 구체성은 입력된 근거와 업무 연결에서 나온다'(통제감·근거결박 톤). docstring '생존'/'상위 5%'도 정리. **Codex 적대검증: 절대규칙/설계원칙·금칙어·format 플레이스홀더 회귀 없음 확인** + 추가 정직성 fix 2건: ① `evidence_reason_ko`를 모델출력 우선 → **검증된 driver값으로 강제 결박**(evidence_title과 동일, 모델 부풀림 차단 — 조언=모델/근거=검증값 분리) ② `fallback_plan` docstring 거짓('항상 guardrail_ok=True') → 실제(무근거 시 False)대로 정정. 35테스트 통과(test_actionplan_* 포함), 시크릿 clean. **다음:** 배포 준비(README/DEPLOY 점검) 또는 O*NET 캘리브레이션(손추정→calibrated).
- **P3 (액션플랜 잠금 ↔ 패키지 오퍼 정합성, 3-AI 루프) ✅:** 잠긴 2·3번이 '🔒 잠금 — 대응법 보기'(돈 내면 텍스트 더 = commodity 함정)로 읽혀 99,000원 결과물 패키지와 불일치하던 걸 정리. **Gemini 카피 재설계:** 텍스트(주간 방향)=무료 미끼/리텐션, 유료=결과물(이력서 재설계)로 메시지 일관화. `_action_plan_html` 재작성 — 잠긴 제목 **blur 제거(방향=맛보기 노출)** + 잠금 카피를 '이 방향을 내 이력서 요약·성과 불릿 초안으로 — 운영자가 직접 검토해 반영(결과물)'로 + 섹션 서브타이틀 + 무료 '이번 주 핵심 방향' 배지 + 섹션명 '생존 액션플랜'→'액션플랜'(공포 톤 완화) + CTA를 '위 방향, 내 이력서로 만들까요?'로 액션플랜↔패키지 브릿지. **Codex 적대리뷰 fix-first 3건:** ① **`/offer` 직링크가 guardrail_ok 무시 → 무근거 직무도 결제화면 도달**(제품레벨 무근거 판매 구멍): offer_html에 `grounded` 파라미터 추가, server `/offer`가 리포트와 동일 기준으로 판정해 무근거면 '준비 중' 정직 안내(결제버튼 없음) ② 잠금 카피 '상세 단계'가 여전히 '텍스트 더' 뉘앙스 → 결과물만 말하게 ③ '전문가가 직접 적용' 1인 MVP 과장 → '운영자가 직접 검토' + **fail-open `guardrail_ok` 기본값 → fail-closed**(플래그 누락 캐시를 무근거로 간주). 라이브(무근거 직무 오퍼 게이팅·CTA 숨김)+35테스트 통과, 시크릿 clean. 샘플 `web/sample-report.html`. **다음:** actionplan.py 내부 프롬프트 '상위 5% 대응법'(생성 오염 소지) 정리 / 배포(호스팅·PG·카카오, 사용자 필요).
- **P2 (Phase 5 — 결제 검증 웹훅 배선, 3-AI 루프) ✅:** 30일 테스트 핵심지표=실결제인데 앱이 결제완료를 몰랐던 구멍을 닫음(Codex·Gemini 공통 '단 하나의 다음 액션'). `store.py` 결제 WIP(save_payment/_reduce_payments/paid_count, 상태랭크 reported<failed<paid<refunded) 활용 + `server.py`에 배선: **POST `/webhook/payment`**(PG 서버→서버, `PAYMENT_WEBHOOK_SECRET` HMAC-SHA256 원본bytes 서명검증=`_payment_sig_ok` compare_digest, 통과 시에만 paid) · **GET `/payment/success`**(클라이언트 리다이렉트=조작가능 → 아무것도 저장 안 함, '접수 확인' 페이지 `report.payment_pending_html`만) · `/health`에 `paid_customers`(서명검증 paid만, reported≠paid). `data/payments.jsonl` .gitignore. **정직성 불가침 구현:** 시크릿 없으면 501(paid 절대 안 셈), success URL 조작으로 paid 못 부풀림. **Codex 적대 보안리뷰 fix-first 3건:** ① 금액 검증 추가(`_finalize_pay_status` — 서명O라도 amount 0/음수/기대가(`PAYMENT_EXPECTED_AMOUNT`=99000) 불일치면 failed로 강등, 무료/타상품 이벤트 차단) ② status 매핑 좁힘(approved/authorized=승인≠캡처 제외, terminal-paid `done/paid/completed`만) ③ `/payment/success` 공개 append-only DoS 차단(리다이렉트 저장 제거). + 서명헤더 비-hex 방어, save_payment extra가 core필드 못 덮게. 라이브 E2E(위조서명401·0원/5만원/approved→failed·success폭격 무저장·멱등) + 35테스트 통과, 시크릿 clean. **다음:** 액션플랜 잠금(2·3 텍스트)과 패키지 오퍼 기대불일치 정리 / 배포 런북에 PG 웹훅 연결 절차.
- **P1 (Phase 1 — 오퍼 재정의, 3-AI 루프) ✅:** 런칭 로드맵을 Gemini(제품/UX)+Codex(적대리뷰) 종합으로 확정(만장일치: 코드 멈추고 30일 내 지불주체 1명 증명, 파는 건 지수 아닌 '손에 남는 결과물'). **오퍼를 '주간 텍스트 플랜 월 구독(9,900/월, =곧 commodity)'에서 → '`AI 시대 커리어 재설계 패키지`(99,000원 1회성)'로 재정의**: 결과물=데이터 기반 직무진단 리포트+사람 고유 역량 중심 이력서 재배치 가이드+커리어 디렉터가 직접 다듬은 이력서 초안+1~3년 디펜스 플랜. 사람-검토 정직 표기, 진행 4단계, 실결제/사전신청 이원화. `src/report.py` offer_html 재작성 + 리포트 CTA 정합. **Codex 적대리뷰 fix-first 5건(정직성·법무 직격):** ① 환불 문구 위험("작업 시작 후 환불 불가"가 전자상거래법 청약철회권 죽임) → "법정 청약철회권·계약불일치 환불권 보장, 개시분만 법령 범위 내 공제" ② **과장 효능('AI-Proof'/'대체 불가' 절대보장 = 표시광고법 리스크) → 'AI 시대'/'상대적으로 덜 대체되는 사람 고유 역량'으로 완화** ③ '주 5명' 인위적 희소성 우려 → "초기에는 주 최대 5명 수동 검토" 톤(가짜 카운터 아님 명시) ④ PII 동의문에 수집항목+거부권·불이익 고지 추가(PIPA §15) ⑤ `real=bool(payment_url)` 버그(javascript: 등 악성 URL이 실결제 모드로 샘) → `_safe_url` 후 `!="#"`로 판정. + 결제완료 자동확인 안 됨 정직 고지. 30테스트 통과, 시크릿 스캔 clean. 미리보기 `web/sample-offer.html`(사전신청)·`web/sample-offer-paid.html`(실결제). **다음(Phase 5 후보):** 결제 웹훅/성공콜백 배선(`store.py`에 save_payment WIP 존재) + 액션플랜 잠금(2·3 텍스트)과 패키지 오퍼의 기대불일치 정리.
- **V3 (런칭 런북 + 측정 도구 + 정직성 하드닝) ✅:** `src/interest.py`(사전예약 리드 측정 CLI — 총/직무별/가격별/최근, 연락처 마스킹) + DEPLOY.md §2.5 런칭 런북(1단계 리드검증·2단계 결제검증)·§2.6 측정. **Codex 적대리뷰 6건 — 정직성·PII·법무 직격, fix-first:** ① **PAYMENT_URL은 외부링크 보내기만 → 결제완료를 앱이 모름**: "결제이벤트로 WTP 측정"은 아직 미구현임을 DEPLOY에 정직 명시(결제 웹훅/`payments` 저장 = 다음 TODO) ② **국내 PG 현실**(토스 사업자+심사~14일, Stripe 한국 미지원) 런북에 반영, Stripe 제거 ③ **개인정보처리방침을 PII 수집 전 선행 게이트로** 격상(PIPA) ④ CSV PII: 마스킹 기본 + `--raw`만 평문(stderr 경고) + 수식인젝션 방어 ⑤ 무염 SHA256 IP는 역추적 가능 → **솔트 HMAC(`INTEREST_SALT`), 솔트 없으면 IP 미저장**, 오해소지 주석 수정 ⑥ `/health`와 CLI 카운트 분기 → 둘 다 유효 JSON 기준 통일. 좀비 테스트서버 정리. 30테스트 통과. **다음:** 결제 웹훅/성공콜백(`/payment/success`,`/webhook/payment`+payments 저장)으로 진짜 지불검증 닫기 / 오퍼 카피 Gemini 다듬기 / 개인정보처리방침·약관 페이지 템플릿.
